# 任务空间（Task Space）

PomodoroXII 的任务空间上下文：三层工作项树、依赖域与专注会话。
本文件是术语表 —— 只记录术语与定义，不记录规格、实现或决策过程。

## 度量

**完成度（Completion Progress）**：
「工作项完成了多少」的度量，只由有效直接子项的完成情况决定（完成 / 未完成 / 取消）。
P0 口径：只做计数呈现（已完成 X / 有效共 Y），只统计直接子项，不折算百分比。
_Avoid_: 进度、进展、完成百分比 —— 单说「进度」时歧义最大，禁止裸用

**有效子项（Valid Child）**：
未被取消的直接子项；取消项从完成度分母中排除，重开后重新进入。
_Avoid_: 未完成子项（那只是有效子项里未完成的那部分）

**N/A（完成度）**：
完成度分母为空的状态 —— 存在直接子项但全部取消，或尚无直接子项。
不折算为百分比，也不作为字串出现在界面上。
_Avoid_: 0%、未开始、空进度

**范围已取消（Scope Cancelled）**：
存在直接子项且有效子项为 0 时，完成度的标记。
_Avoid_: 100%、已完成、取消进度

**投入（Effort）**：
「花了多少时间」的度量：预计投入区间（估算）与实际投入（会话净时长）；只归二级工作项。
_Avoid_: 进度、计时进度

**会话总体推进（Session Overall Progress）**：
会话结束时用户对本次推进的主观评分（`smooth` / `progressed` / `stuck` / `interrupted`）。
与工作项树的完成度之间没有任何派生关系。
_Avoid_: 进度、会话进度、总体进度

## 状态与依赖

**等待（Waiting）**：
指**类目为 waiting 的状态**（不是某个具体状态）—— 一个空间可能有多个等待状态，含用户自定义的。
进入等待由用户显式选择，常见原因是它的上游依赖还没完成；判定一律按状态的**类目**，不按状态名或固定 id。
进不进入等待是人的决定，系统不会替用户改。
依赖全部解除（上游完成，或上游取消后已**确认不再需要**）时，系统只会**提示**可以恢复，绝不自动改状态。
_Avoid_: 被阻塞（那是派生信号，见下条）、自动等待、系统置为等待

**被阻塞（Blocked）**：
工作项的**派生信号**：上游依赖未完成时系统算出的标记。
只有二级工作项有它；依赖完成即自动消失，用户不能直接开关它。
_Avoid_: 等待、阻塞状态 —— 把派生信号说成"状态"会让人以为要手动设置

**需要解决（Needs Resolution）**：
单条依赖边的派生信号：上游已**取消**、但用户还没有确认「不再需要」。
取消**不等于**完成 —— 这样的上游依然阻塞下游，直到显式确认（见下条）或解除依赖。
_Avoid_: 取消即完成、自动解除、需要删除

**已确认不再需要（Confirmed Not Required）**：
用户对一条「需要解决」依赖的**显式决定**：上游虽然取消了，但它的产出确实不再需要。
它是随同步跨设备一致的**服务端事实**，不是本地记忆；确认后该边视为已满足并保留审计边，
下游的阻塞/提示随之重算 —— 状态本身不会因此自动变化。
_Avoid_: 自动确认、忽略、跳过

**等待前态（Pre-Waiting Status）**：
某工作项**最后一次进入「等待」之前所处的状态**。
它是需要被记住的**事实**，不是派生值；离开「等待」后仍然保留，只在处于「等待」时才被消费。
_Avoid_: 上一个状态（歧义：可能被读成任意相邻的两个状态）、原状态、停等前的进度
