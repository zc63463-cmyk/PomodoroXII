# 全局 8 阶段战略指导 v1

> **生成时间**: 2026-07-05
> **执行依据**: [MCP服务跟踪与战略指导-续作计划-v2.md](./MCP服务跟踪与战略指导-续作计划-v2.md) 阶段 D'
> **当前分支**: `codex/mcp-wip` (commit 67d78c9)
> **基线**: 544+ tests passed / main CI 绿
> **范围**: 后端收口为主（阶段 4-6 详细），前端为辅（阶段 7-8 简略）
> **关联**: [MCP服务跟踪-项目状态全景报告.md](./MCP服务跟踪-项目状态全景报告.md)

---

## 1. 执行摘要

基于 [MCP服务跟踪-项目状态全景报告.md](./MCP服务跟踪-项目状态全景报告.md) 的代码验证结果，PomodoroXII 项目当前实际进度：

| 阶段 | 状态 | 关键结论 |
|---|---|---|
| 阶段 1: MCP 收口 | ✅ 已完成 | 46 个 MCP 工具齐全 |
| 阶段 2: HTTP lifespan | ✅ 已完成 | `asyncio.run(init_meta_db())` 已显式调用 |
| 阶段 3: lint cleanup | ✅ 已完成 | ruff 配置完整 + MCP WIP 文件已 exclude |
| 阶段 4: Spec 化 EXPECTED_MCP_TOOLS | ⚠️ 需重新评估 | STAT_SPECS 已替代 stats parity |
| 阶段 5: 部署基线 GHCR | ⚠️ CI 已配置，需验证首次推送 | `packages: write` 权限已就绪 |
| 阶段 6: 生产安全加固 | ❌ 未实现（P0） | 限流 + 安全头缺失 |
| 阶段 7: 前端 MVP | ❌ 未开始 | 仅给技术栈选型建议 |
| 阶段 8: 长期扩展 | ❌ 未开始 | 简略清单 |

**核心建议**: 阶段 1-3 已完成，**直接进入阶段 4-6**，其中阶段 6（安全加固）为 P0。

---

## 2. 阶段 1-3 完成证据（仅记录）

### 2.1 阶段 1: MCP 收口 — ✅ 已完成

**证据**:
- [server.py:82-91](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/mcp/server.py#L82-L91) `FastMCP("PomodoroXII", instructions=...)` 构造正常
- CBM `search_graph(label="Function", file_pattern="*mcp*")` 返回 46 个 MCP 函数
- 工具涵盖 Space discovery / Statistics / Meta / Sync 等域
- `mcp.name` 已正确设置为 "PomodoroXII"

**后续行动**: 无

### 2.2 阶段 2: HTTP lifespan TDD 修复 — ✅ 已完成

**证据**: [server.py:428-439](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/mcp/server.py#L428-L439)
```python
asyncio.run(init_meta_db())  # 显式初始化
try:
    if args.transport == "http":
        mcp.run(transport="http", host=args.host, port=args.port)
    else:
        mcp.run()
finally:
    asyncio.run(dispose_space_engine_manager())
    asyncio.run(close_meta_db())
```

**实现方式**: 直接在 `main()` 中显式调用，比原计划"方案 2 main 手动 init/cleanup"更直接。finally 块确保资源清理。

**后续行动**: 无（可选：补 TDD 测试覆盖 lifespan，但非阻塞）

### 2.3 阶段 3: lint cleanup — ✅ 已完成

**证据**: [pyproject.toml:43-61](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/pyproject.toml#L43-L61)
- `[tool.ruff]` + `exclude = ["app/mcp/**", "tests/test_mcp_server.py"]`（保护 MCP WIP）
- `[tool.ruff.lint]` 选择 `F/E/W/I`，忽略 `E501/E722/E402`
- `[tool.ruff.lint.per-file-ignores]` 放宽 tests 目录
- CI 已配置 ruff 为 blocking

**后续行动**: 无

---

## 3. 阶段 4: Spec 化 EXPECTED_MCP_TOOLS — 重新评估

### 3.1 原计划

提取 `EXPECTED_MCP_TOOLS` 常量做 MCP 工具 parity gate，确保 REST/MCP 工具一致性。

### 3.2 实际状态

- `EXPECTED_MCP_TOOLS` **未实现**（grep 无结果）
- 但 `STAT_SPECS + StatSpec`（[stats_spec.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/stats_spec.py)）已实现 **7 维度 stats parity**：
  - 统一登记：`name` / `service_method` / `route_path` / `mcp_tool` / `params`
  - 替代原先三处手工同步
- `parity_helpers.py` 的 `get_registered_mcp_tool_names()` 已用 `mcp.list_tools()` 自省（[审计报告](file:///e:/Development/MyAwesomeApp/PomodoroXII/审计报告/扩展性4.5星提升-修复优化工作报告.md) §3.4 确认）
- `test_parity_stats_mcp.py` + `test_stat_spec.py` 已驱动 STAT_SPECS 覆盖全部 REST 端点和 MCP 工具

### 3.3 建议行动

**P2（可选补强）**: 评估是否仍需 `EXPECTED_MCP_TOOLS`。

**判断标准**:
- 若 `STAT_SPECS` + `mcp.list_tools()` 自省已覆盖所有 stats 工具 parity → **关闭此阶段**
- 若需补强非 stats 工具（如 `list_all_spaces`）的 parity → 提取到 `EXPECTED_MCP_TOOLS` 常量，添加 parity test

**推荐**: 由于 `mcp.list_tools()` 自省已能动态发现所有注册工具，且 `STAT_SPECS` 已覆盖 stats 域，**建议关闭阶段 4**，避免引入新的双重事实源。

---

## 4. 阶段 5: 部署基线 GHCR — CI 已配置，需验证首次推送

### 4.1 CI 配置已就绪

[ci.yml:108-143](file:///e:/Development/MyAwesomeApp/PomodoroXII/.github/workflows/ci.yml#L108-L143):
```yaml
permissions:
  packages: write

- name: Build and push image to GHCR
  uses: docker/build-push-action@v5
  with:
    context: ./backend
    push: true
    tags: |
      ghcr.io/${{ github.repository_owner }}/pomodoroxii-backend:${{ github.sha }}
      ghcr.io/${{ github.repository_owner }}/pomodoroxii-backend:latest
```

### 4.2 待验证清单

| # | 验证项 | 验证方式 | 预期结果 |
|---|---|---|---|
| 1 | GitHub PAT scope | `gh auth status` 或 GitHub Settings → Developer settings → PAT | `write:packages` scope 已启用 |
| 2 | 首次推送触发 CI | `git push origin main` | CI build job 成功完成 |
| 3 | GHCR 镜像生成 | 访问 `https://github.com/users/zc63463-cmyk/packages/container/pomodoroxii-backend` | 镜像存在，tag 为 latest + commit sha |
| 4 | 镜像可拉取 | `docker pull ghcr.io/zc63463-cmyk/pomodoroxii-backend:latest` | 拉取成功，size 200-400 MB |
| 5 | 镜像可运行 | `docker run -p 8000:8000 ghcr.io/zc63463-cmyk/pomodoroxii-backend:latest` | `/api/health` 返回 200 |

### 4.3 P1 行动项

1. 推送 main 触发 CI
2. 检查 GHCR 镜像生成
3. 拉取验证
4. 运行 smoke test

### 4.4 风险

| 风险 | 缓解 |
|---|---|
| PAT 缺 `write:packages` scope | 重新生成 PAT 或使用 `GITHUB_TOKEN` 自动 token |
| 镜像 size 过大 | 优化 Dockerfile（multi-stage build） |
| 镜像运行失败 | 检查 entrypoint + env vars |

---

## 5. 阶段 6: 生产安全加固 — P0（未实现）

### 5.1 未实现的 P0/P1 项

基于 [pomodoroxii-deep-review-report.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/pomodoroxii-deep-review-report.md) §四：

| 项 | 严重度 | 验证 | 建议实现 |
|---|---|---|---|
| 认证限流（M1） | P0 | grep `slowapi\|RateLimit` 无结果 | `slowapi` 5次/分钟/IP 对 `/auth/login` |
| 安全响应头（M2） | P0 | grep `SecurityHeadersMiddleware` 无结果 | 中间件注册 |
| sync payload schema 校验（M3） | P1 | 需重新验证 [sync.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/sync.py) | Pydantic schema 替代 `dict[str, Any]` |
| 弱密钥黑名单（M5） | P1 | 需重新验证 [auth/security.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/auth/security.py) | 扩展黑名单或 `len(secret_key) >= 32` |
| setup 竞态（M4） | P2 | 需重新验证 [auth.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/auth.py) | 捕获 `IntegrityError` 返回 409 |

### 5.2 P0 行动项 1: 认证限流

**实现方案**:

1. 添加依赖（[pyproject.toml](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/pyproject.toml)）:
```toml
[project.optional-dependencies]
dev = [
    # ... existing
    "slowapi>=0.1.9",
]
```

2. 配置 limiter（新建 `app/middleware/rate_limit.py`）:
```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
```

3. 在 [main.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/main.py) 注册:
```python
from app.middleware.rate_limit import limiter
from slowapi import _rate_limit_exceeded_handler
from slowapi.errors import RateLimitExceeded

app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
```

4. 在 [auth.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/auth.py) 应用限流:
```python
from app.middleware.rate_limit import limiter

@router.post("/login")
@limiter.limit("5/minute")
async def login(request: Request, ...):
    ...
```

5. 添加测试（`tests/test_rate_limit.py`）:
- `test_login_rate_limit_5_per_minute`
- `test_login_rate_limit_reset_after_window`

### 5.3 P0 行动项 2: 安全响应头

**实现方案**:

新建 `app/middleware/security_headers.py`:
```python
from starlette.middleware.base import BaseHTTPMiddleware

class SecurityHeadersMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        response = await call_next(request)
        response.headers["X-Content-Type-Options"] = "nosniff"
        response.headers["X-Frame-Options"] = "DENY"
        response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
        response.headers["X-XSS-Protection"] = "1; mode=block"
        response.headers["Referrer-Policy"] = "strict-origin-when-cross-origin"
        return response
```

在 [main.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/main.py) 注册:
```python
from app.middleware.security_headers import SecurityHeadersMiddleware
app.add_middleware(SecurityHeadersMiddleware)
```

添加测试（`tests/test_security_headers.py`）:
- `test_x_content_type_options`
- `test_x_frame_options`
- `test_hsts_header`
- `test_xss_protection`
- `test_referrer_policy`

### 5.4 P1 行动项: sync payload schema 校验

**当前状态**: [sync.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/sync.py) push 入口接受 `dict[str, Any]`，可能覆盖任意字段。

**建议**: 用 Pydantic schema 替代，字段白名单：
```python
class SyncEventSchema(BaseModel):
    id: str
    entity_type: str
    action: Literal["create", "update", "delete"]
    payload: dict[str, Any]
    timestamp: str
    version: int | None = None
```

**注意**: `strip_client_fields()` 已在 [sync.py:229](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/sync.py#L229) 实现，剥离客户端独有字段。Pydantic schema 是额外防护层。

### 5.5 阶段 6 验证清单

- [ ] `slowapi` 依赖已添加
- [ ] `/auth/login` 限流 5次/分钟/IP
- [ ] `SecurityHeadersMiddleware` 已实现并注册
- [ ] 安全头测试全部通过
- [ ] 限流测试全部通过
- [ ] sync payload Pydantic schema 已评估（可选）

---

## 6. 阶段 7: 前端 MVP — 未开始（建议性）

**说明**: 本阶段为建议性，不在当前执行范围内。详细实施计划待阶段 6 完成后单独规划。

### 6.1 用户偏好

- 技术栈: React > Vue3
- 布局风格: memos-style
- 视觉细节: subtle shadows + unified borders
- 小记 feature: 专注模式行为（preview、expand/collapse editing area、hide/show notes when clicking entries）

### 6.2 技术栈选型建议

| 子阶段 | 技术栈 | 备注 |
|---|---|---|
| 7.1 脚手架 | Next.js 15 (App Router) + Tailwind CSS 4 + shadcn/ui | 用户偏好 React |
| 7.2 认证 | NextAuth.js v5 + 双 JWT（master + space） | 复用后端 `/auth/*` |
| 7.3 番茄钟核心 | React Server Components + Server Actions | 调用 `/api/v1/sessions/*` |
| 7.4 小记 feature | memos-style 布局 + subtle shadows | 用户明确偏好 |
| 7.5 反思/习惯/快捷笔记 | 复用后端 routes/v1/* | — |
| 7.6 同步 UI（可选） | 调用 `/api/v1/sync/*` | — |
| 7.7 部署 | Vercel (前端) + Cloudflare Tunnel (后端) | 与阶段 5 GHCR 协同 |

### 6.3 关键集成点

1. **NextAuth.js + 双 JWT**: 后端 `/auth/login` 返回 master token + space token，NextAuth Credentials Provider 调用后端验证
2. **多空间切换**: 前端需维护当前 space_id 状态，所有 API 调用带 `X-Space-Id` header
3. **memos-style 小记**: 需实现专注模式行为（user_profile 偏好）

### 6.4 风险

| 风险 | 级别 | 缓解 |
|---|---|---|
| NextAuth.js v5 与后端双 JWT 集成复杂度 | P2 | 单独规划，参考 NextAuth.js v5 + Credentials Provider 文档 |
| 前端状态管理（space 切换） | P2 | 使用 Zustand 5（user_profile 偏好） |
| memos-style 布局实现 | P2 | 参考 usememos/memos 开源项目 |

---

## 7. 阶段 8: 长期扩展 — 未开始（简略清单）

| 项 | 触发条件 | 优先级 | 备注 |
|---|---|---|---|
| APScheduler（备份 + snapshot） | 阶段 5 完成 | P1 | 参考 `fabioferreira/apscheduler-fastapi` 模式 |
| backup_service / snapshot_service / consistency_service | 阶段 6 完成 | P1 | [pomodoroxii-deep-review-report.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/pomodoroxii-deep-review-report.md) §1.2 |
| frontmatter.py 实现（.md YAML frontmatter） | 阶段 7.4 完成 | P1 | .md 文件添加 id/title/tags/folder_id/content_hash/created_at/updated_at |
| 8 个内联 Service 移入 services/ 目录 | ✅ 已完成 | — | CBM 确认全部 18 个 Service 已在 `app/services/` 下 |
| export/admin/search 路由 | 阶段 7 完成 | P2 | [pomodoroxii-deep-review-report.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/pomodoroxii-deep-review-report.md) §1.2 |
| Goal 落库 + GoalService | 阶段 7 稳定 | P2 | 使用 `backend/scripts/scaffold_entity.py` 脚手架 |
| MCP CRUD tool（从 read-only 扩展为 read-write） | 阶段 7 稳定 | P2 | 需评估安全风险 |
| Sync UI（可视化冲突解决） | 阶段 7.6 完成 | P2 | — |
| Multi-tenant（单用户 → 多租户） | 商业化需求 | P3 | 架构层面重构 |

---

## 8. 阶段依赖图（基于 CBM 验证）

```
[已完成] 阶段1(MCP收口) ──→ [已完成] 阶段2(HTTP lifespan) ──→ [已完成] 阶段3(lint)
                                                                          │
                                                                          ↓
[待评估] 阶段4(Spec化) ←─────────────────────────────────────────────────┘
                │
                ↓
[待验证] 阶段5(GHCR推送) ──→ [未实现,P0] 阶段6(安全加固) ──→ [未开始] 阶段7(前端MVP,建议性)
                                                              │
                                                              ↓
                                                      [未开始] 阶段8(长期扩展)
```

### 8.1 关键依赖关系

| 依赖 | 说明 |
|---|---|
| 阶段 4 → 阶段 5 | Spec 化完成后，CI 才能稳定推送 GHCR |
| 阶段 5 → 阶段 6 | 安全加固需在 GHCR 镜像基础上验证 |
| 阶段 6 → 阶段 7 | 前端 MVP 需要后端安全基线就绪 |
| 阶段 7 → 阶段 8 | 长期扩展需前端稳定 |

### 8.2 可并行项

- 阶段 4（Spec 化评估）可与阶段 5（GHCR 推送）并行
- 阶段 6（安全加固）的限流和安全头可并行实现
- 阶段 8 的 APScheduler 可与阶段 7 并行

---

## 9. 风险矩阵（覆盖阶段 4-8）

| ID | 风险 | 阶段 | 级别 | 缓解 |
|---|---|---|---|---|
| R1 | GitHub PAT 缺 `write:packages` scope | 5 | P0 | 验证 PAT scope，必要时重新生成或使用 `GITHUB_TOKEN` |
| R2 | `slowapi` 与 async FastAPI 兼容性 | 6 | P1 | 验证 `slowapi>=0.1.9` 支持 async；备选 `fastapi-limiter` |
| R3 | 前端 NextAuth.js 与后端双 JWT 集成复杂度 | 7 | P2 | 单独规划，参考 NextAuth.js v5 + Credentials Provider |
| R4 | APScheduler 与 FastAPI lifespan 集成 | 8 | P1 | 参考 `fabioferreira/apscheduler-fastapi` 模式 |
| R5 | sync payload Pydantic schema 校验可能破坏现有客户端 | 6 | P1 | TDD 先行，保留 `dict[str, Any]` 回退 |
| R6 | 6 实体删除墓碑创建状态未验证 | 6 | P1 | 重新验证 [pomodoroxii-deep-review-report.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/pomodoroxii-deep-review-report.md) §3.1 是否已修复 |
| R7 | frontmatter.py 实现可能破坏现有 .md 文件 | 8 | P1 | TDD + 渐进迁移，保留纯文本回退 |
| R8 | Cognee 云端 API 不稳定（已规避） | — | — | 已跳过 Cognee，仅用 CBM + 文档直读 |
| R9 | 镜像 size 过大 | 5 | P2 | 优化 Dockerfile（multi-stage build） |
| R10 | 前端状态管理复杂度 | 7 | P2 | 使用 Zustand 5（user_profile 偏好） |

---

## 10. 优先级建议

### 10.1 P0（立即执行）

1. **阶段 5 GHCR 推送验证**
   - 推送 main 触发 CI
   - 验证 GHCR 镜像生成
   - 拉取验证

2. **阶段 6 认证限流**
   - 添加 `slowapi>=0.1.9` 依赖
   - 对 `/auth/login` 限流 5次/分钟/IP
   - 添加测试

3. **阶段 6 安全响应头**
   - 实现 `SecurityHeadersMiddleware`
   - 在 [main.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/main.py) 注册
   - 添加测试

### 10.2 P1（1-2 周内）

1. **阶段 4 评估** — 判断是否关闭（STAT_SPECS 已覆盖）
2. **阶段 6 sync payload schema 校验** — Pydantic schema + 字段白名单
3. **阶段 6 弱密钥黑名单扩展** — `len(secret_key) >= 32`
4. **阶段 8 APScheduler** — 定时备份 + snapshot
5. **阶段 8 backup/snapshot/consistency service** — 补全架构规划模块

### 10.3 P2（后续迭代）

1. **阶段 7 前端 MVP** — Next.js 15 + Tailwind + shadcn/ui
2. **阶段 8 frontmatter.py** — .md YAML frontmatter
3. **阶段 8 export/admin/search 路由**
4. **阶段 8 Goal 落库** — 使用 `scaffold_entity.py`
5. **阶段 8 MCP CRUD tool** — 从 read-only 扩展为 read-write

### 10.4 P3（商业化需求）

1. **阶段 8 Multi-tenant** — 单用户 → 多租户

---

## 11. 退出条件

本战略指导文档的退出条件：

1. ✅ 阶段 1-3 完成证据完整（含代码引用）
2. ✅ 阶段 4 重新评估结论明确（建议关闭）
3. ✅ 阶段 5 GHCR 验证清单可执行（5 个验证项）
4. ✅ 阶段 6 P0 行动项含代码示例（slowapi + SecurityHeadersMiddleware）
5. ✅ 阶段 7-8 为建议性，不展开实施细节
6. ✅ 阶段依赖图基于 CBM 验证
7. ✅ 风险矩阵覆盖全部 8 阶段（R1-R10）
8. ✅ 优先级建议分 P0/P1/P2/P3 四级

---

> **文档生成依据**: CBM 代码知识图谱 (1810 nodes / 9160 edges) + 13 份核心文档 + 4 份审计报告 + 代码 grep/read 验证。所有代码引用使用 `file:///` 协议可点击跳转。详细项目状态见 [MCP服务跟踪-项目状态全景报告.md](./MCP服务跟踪-项目状态全景报告.md)。
