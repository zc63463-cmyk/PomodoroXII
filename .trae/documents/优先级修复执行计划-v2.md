# PomodoroXII 优先级修复执行计划 v2

> **生成时间**: 2026-07-04 (Plan Mode Phase 3)
>
> **关联文档**: 详细任务规划见 [优先级修复与PhaseC实施计划.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/.trae/documents/优先级修复与PhaseC实施计划.md) (809 行)
>
> **方法论**: TDD (Red→Green→Refactor), Skill: `test-driven-development` 引导; `agent-browser` 可选用于 C10 端到端测试
>
> **执行约束**: 当前 Plan Mode 已重置，本计划是本次会话的执行指南。

---

## 一、Phase 1 探索成果（已验证）

### 1.1 关键源文件状态（2026-07-04 重新读取确认）

| 探索项 | 状态 | 证据 |
|--------|------|------|
| `project_memory.md` 是否存在 | ❌ **不存在** | `c:\Users\20564\.trae-cn\memory\projects\-e-Development-MyAwesomeApp-PomodoroXII\` 下仅日期目录 + topics.md |
| 详细计划文件 `优先级修复与PhaseC实施计划.md` 是否存在 | ✅ 存在 (809 行) | `.trae/documents/优先级修复与PhaseC实施计划.md` |
| `BaseService.list()` 返回类型 | ✅ 已返回 `(items, total)` 元组 | [base.py:55-67](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/base.py#L55-L67) |
| `BaseService.update()` 是否增加 version | ❌ 不增加 version | [base.py:69-77](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/base.py#L69-L77) 仅设 `obj.updated_at` |
| `PaginatedResponse[T]` schema 是否存在 | ✅ **已存在** | [common.py:10-23](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/schemas/common.py#L10-L23) — 但路由层未使用 |
| P2-1 影响范围 | 11 处 `return items` | reflections.py:100, notes.py:57, quick_notes.py:91, habits.py:117+182, folders.py:85, schedules.py:77, sessions.py:80, time_blocks.py:78, tasks.py:58, trash.py:111 |
| P2-5 sync infra 模型索引 | ❌ 无 `index=True` | sync_outbox.py L25-31 + sync_audit_log.py L24-29 全部无索引 |
| P2-6 Mixin updated_at onupdate | ❌ 未实现 | [mixins.py:28](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/models/mixins.py#L28) 无 onupdate |
| P2-6 Mixin version 自增 | ❌ 未实现 | [mixins.py:29](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/models/mixins.py#L29) 仅 default=1 |
| Phase C 旧计划代码完整性 | ✅ 完整可用 | [phase-c-sync-completion-plan.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/.trae/documents/phase-c-sync-completion-plan.md) L99-524，但路径针对 `pomodoroxi-rebuild`（已归档） |
| NoteService Saga 现状 | ✅ 完整 Try-Compensate | [note.py:94-102](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/note.py#L94-L102) — 但**没有 `sync_mode` 参数** |
| `space_session` / `client` fixture | ✅ 已存在 | [conftest.py:87-109](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/tests/conftest.py#L87-L109) + L112-137 |
| TombstoneService 现状 | ✅ 含 TOCTOU 修复 | [tombstone.py:42-52](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/tombstone.py#L42-L52) |
| routes/v1/__init__.py 现状 | ✅ 15 路由挂载 | [__init__.py:37-59](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/__init__.py#L37-L59) — 无 sync_router |
| EntitySpec dataclass | ✅ frozen dataclass | [entities.py:52-73](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/entities.py#L52-L73) |

### 1.2 关键架构约束（铁律）

1. **三层铁律**: Routers commit / Services flush / Models 纯数据 — Service 不导入 fastapi，不调 commit
2. **Note 模型无 content 字段**: .md 文件是唯一 Source of Truth，ORM 仅存 `content_hash` + `word_count`
3. **双 JWT 认证**: Master Token(7d) + Space Token(8h, 含 space_id)
4. **双 Base 隔离**: `app.db.base.Base` (业务) vs `app.file_system.schema.Base` (FS 索引)
5. **Saga Try-Compensate**: NoteService create/update_content/delete 三方法均含补偿

### 1.3 测试基线

- **当前测试数**: 214 个测试 / 36 文件
- **测试框架**: pytest + pytest-asyncio (asyncio_mode=auto)
- **隔离机制**: `conftest.py` 的 `_isolate_env` autouse fixture
- **关键 fixture**: `space_session` (per-test 空间 DB), `client` (httpx AsyncClient + ASGITransport)

---

## 二、P0 阶段 — 立即修正记忆

### 2.1 任务

创建 `c:\Users\20564\.trae-cn\memory\projects\-e-Development-MyAwesomeApp-PomodoroXII\project_memory.md`，明确标注 Phase C 实际进度 0%、测试总数 214、前端不存在等关键事实，避免后续 Agent 被 cognee 错误信息误导。

### 2.2 操作

**工具**: Write 新建文件

**内容**: 见详细计划 [优先级修复与PhaseC实施计划.md L63-137](file:///e:/Development/MyAwesomeApp/PomodoroXII/.trae/documents/优先级修复与PhaseC实施计划.md#L63-L137) 提供的完整模板，包含:
- Phase C 实际进度 = 0%
- 测试总数 = 214（非 244）
- 前端不存在
- 无 CI/CD 且 Git 未初始化
- 旧文档针对已归档项目
- 真实架构与 Phase 进度表

### 2.3 验证

1. `Read` 文件应存在
2. 内容包含 "Phase C 实际进度 = 0%" 和 "测试总数 = 214"

### 2.4 不做的事

- ❌ 不修改 session_memory_*.jsonl
- ❌ 不修改 topics.md
- ❌ 不修改 user_profile.md

---

## 三、P1 阶段 — Phase C 前置三项

### 3.1 P2-1: 11 个 list 端点丢弃 total

**策略**: 复用已有 `PaginatedResponse[T]` schema，路由层只需:
1. 从 `app.schemas.common` 导入 `PaginatedResponse`
2. 将 `response_model=list[XxxResponse]` 改为 `response_model=PaginatedResponse[XxxResponse]`
3. 将 `return items` 改为 `return {"items": items, "total": total, "limit": per_page, "offset": (page-1)*per_page, "has_more": ((page-1)*per_page + len(items)) < total}`

**涉及 11 处**: tasks.py, notes.py, folders.py, sessions.py, schedules.py, time_blocks.py, reflections.py, quick_notes.py, habits.py (2 处), trash.py

**TDD 流程**: 每个端点先写一个 `test_list_xxx_returns_paginated_envelope` 测试（11 个新测试）

**验证**: `pytest tests/test_routes_v1.py -k paginated -v`

### 3.2 P2-5: SyncOutbox/SyncAuditLog 添加索引

**策略**: 为以下字段添加 `index=True`:
- `SyncOutbox.entity_type`, `entity_id`, `synced_at`, `created_at`
- `SyncAuditLog.event_type`, `entity_type`, `entity_id`, `created_at`

**修改文件**:
- [sync_outbox.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/models/sync_outbox.py) L25-31
- [sync_audit_log.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/models/sync_audit_log.py) L24-29

**新增 Alembic migration**: `alembic/versions/002_sync_indexes.py`

**TDD 流程**: 2 个新测试 (`test_sync_outbox_has_index_on_entity_id`, `test_sync_audit_log_has_index_on_entity_id`)

### 3.3 P2-6: Mixin updated_at onupdate + version 自增

**修改 1**: [mixins.py:28](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/models/mixins.py#L28) 添加 `onupdate=utc_now_iso`

**修改 2**: [base.py:69-77](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/base.py#L69-L77) 在 update 方法添加 `if hasattr(obj, "version"): obj.version = (obj.version or 0) + 1`

**TDD 流程**: 2 个新测试 (`test_update_bumps_version`, `test_update_refreshes_updated_at`)

---

## 四、P2 阶段 — Phase C 实施（10 任务，~54 新测试）

### 4.1 执行顺序

```
C1 sync_safety.py ─── 12 测试, 纯工具函数, 无依赖
    │
    ▼
C2 SyncService.push ─ 5 测试, 依赖 C1
    │
    ▼
C3 SyncService.pull ─ 5 测试, 依赖 C2
    │
    ├──▶ C5 SAVEPOINT 兼容性 ── 3 测试, 纯测试 (可与 C4 并行)
    ├──▶ C8 ENTITY_REGISTRY 验证 ── 3 测试, 纯测试 (可与 C4 并行)
    │
    ▼
C4 SyncService.full + status ── 4 测试, 依赖 C3
    │
    ├──▶ C6 NoteService sync_mode ── 4 测试, 依赖 C3
    │        │
    │        ▼
    └──▶ C9 sync 审计 ── 3 测试, 依赖所有方法
             │
             ▼
       C7 sync 路由 ── 7 测试, 依赖全部
             │
             ▼
       C10 集成测试 ── 8 测试, 使用 agent-browser (可选)
```

### 4.2 各任务摘要（详见详细计划第 4 节）

| 任务 | 文件操作 | 测试数 | 关键约束 |
|------|---------|--------|---------|
| C1 sync_safety.py | Write `app/services/sync_safety.py` | 12 | 不导入 fastapi, 不调 commit |
| C2 SyncService.push | Write `app/schemas/sync.py` + `app/services/sync.py` | 5 | 含 ENTITY_REGISTRY (14 实体) |
| C3 SyncService.pull | Edit `app/services/sync.py` 追加 pull | 5 | Note 走 fs.read_notes_batch |
| C4 full + status | Edit `app/services/sync.py` 追加 | 4 | status 返回 entity_counts |
| C5 SAVEPOINT 兼容性 | Edit `tests/test_note_service.py` 追加 3 测试 | 3 | 无新代码 |
| C6 NoteService sync_mode | Edit `note.py` 添加 sync_mode 参数 + `sync.py` 添加 _push_note_event | 4 | sync_mode=False 默认值, 向后兼容 |
| C7 sync 路由 | Write `app/routes/v1/sync.py` + Edit `__init__.py` 注册 | 7 | 4 端点 push/pull/full/status |
| C8 ENTITY_REGISTRY 验证 | Edit `tests/test_sync_service.py` 追加 3 测试 | 3 | 14 实体完整性门禁 |
| C9 sync 审计 | Edit `app/services/sync.py` 添加 _write_audit | 3 | 在 savepoint 外执行 |
| C10 集成测试 | Write `tests/test_sync_integration.py` | 8 | agent-browser 可选 |

### 4.3 Skill 使用

- **test-driven-development**: 每个 Phase C 任务开始时调用，引导 TDD Red-Green-Refactor 流程
- **agent-browser**: C10 集成测试阶段调用，用于端到端验证 sync 路由（可选，可使用 client fixture 替代）

### 4.4 Phase C 完成验收

```powershell
cd 'e:\Development\MyAwesomeApp\PomodoroXII\backend'
.venv\Scripts\python.exe -m pytest -q
# 预期 214 + ~75 = ~289 测试全绿

# 三层铁律检查（应返回空）
.venv\Scripts\python.exe -c "import pathlib; [print(f) for f in pathlib.Path('app/services').glob('*.py') if 'fastapi' in f.read_text(encoding='utf-8')]"
.venv\Scripts\python.exe -c "import pathlib; [print(f) for f in pathlib.Path('app/services').glob('*.py') if '.commit()' in f.read_text(encoding='utf-8')]"
```

---

## 五、P3 阶段 — 修复剩余 P2/P3 项（6 任务，6 新测试）

| 任务 | 文件 | 测试 |
|------|------|------|
| P2-2 TaskService update 处理 tags | `app/services/task.py` | 1 |
| P2-3 trash.py purge_item N+1 | `app/routes/v1/trash.py` | 1 |
| P2-4 serializers json.loads 保护 | `app/services/serializers.py` | 1 |
| P3-10 Note status CheckConstraint | `app/models/note.py` | 1 |
| P3-11 Task status/priority/due_date 索引 | `app/models/task.py` | 1 |
| P3-13 deps.py space_id 校验 | `app/deps.py` | 1 |

---

## 六、执行计划表（21 任务，~75 新测试）

| # | 阶段 | 任务 | 工具 | 测试 |
|---|------|------|------|------|
| 1 | P0 | 创建 project_memory.md | Write | 0 |
| 2 | P1.1 | P2-1 修复 11 个 list 端点 | Edit 11 文件 | 11 |
| 3 | P1.2 | P2-5 sync infra 添加索引 | Edit 2 文件 + Alembic | 2 |
| 4 | P1.3 | P2-6 Mixin onupdate + version | Edit 2 文件 | 2 |
| 5 | P2.1 (C1) | sync_safety.py | Write + TDD | 12 |
| 6 | P2.2 (C2) | SyncService.push + schemas | Write + TDD | 5 |
| 7 | P2.3 (C3) | SyncService.pull | Edit + TDD | 5 |
| 8 | P2.4 (C4) | SyncService.full + status | Edit + TDD | 4 |
| 9 | P2.5 (C5) | SAVEPOINT 兼容性测试 | TDD only | 3 |
| 10 | P2.6 (C6) | NoteService sync_mode | Edit + TDD | 4 |
| 11 | P2.7 (C7) | sync 路由 | Write + TDD | 7 |
| 12 | P2.8 (C8) | ENTITY_REGISTRY 验证 | TDD only | 3 |
| 13 | P2.9 (C9) | sync 审计 | Edit + TDD | 3 |
| 14 | P2.10 (C10) | 集成测试 | TDD + agent-browser | 8 |
| 15 | P3.1 | P2-2 TaskService update tags | Edit + TDD | 1 |
| 16 | P3.2 | P2-3 trash.py purge N+1 | Edit + TDD | 1 |
| 17 | P3.3 | P2-4 serializers json protection | Edit + TDD | 1 |
| 18 | P3.4 | P3-10 Note status CheckConstraint | Edit + TDD | 1 |
| 19 | P3.5 | P3-11 Task 字段索引 | Edit + TDD | 1 |
| 20 | P3.6 | P3-13 deps space_id 校验 | Edit + TDD | 1 |
| | **合计** | | | **~75 新测试** |

---

## 七、假设与决策

### 7.1 假设

1. `PaginatedResponse` schema 已存在且可直接使用（已验证）
2. Phase C 旧计划代码可用，仅需将路径从 `pomodoroxi-rebuild` 替换为 `PomodoroXII`
3. `space_session` 和 `client` fixture 已就绪（已验证）
4. test-driven-development Skill 提供 TDD 流程引导
5. agent-browser Skill 用于 Phase C 路由端到端测试（可选）
6. 全程遵守三层铁律

### 7.2 关键决策

- **D1**: P2-1 使用已有 `PaginatedResponse` schema，不创建新 schema
- **D2**: P2-5 创建 Alembic migration（002_sync_indexes.py）记录索引变更
- **D3**: P2-6 使用 `hasattr(obj, "version")` 防护，避免影响无 SyncMixin 的模型
- **D4**: Phase C 10 任务严格按依赖顺序执行
- **D5**: 每个任务先写测试（Red），再实现（Green），最后重构（Refactor）
- **D6**: Phase C 完成后更新 project_memory.md 标注进度变化
- **D7**: TDD Skill 在每个 Phase C 任务开始时调用
- **D8**: agent-browser Skill 在 C10 集成测试阶段调用（可选）

### 7.3 不做的事

- ❌ 不修改 session_memory_*.jsonl 或 topics.md
- ❌ 不修改 user_profile.md
- ❌ 不创建除计划文件和必要代码文件之外的新文档
- ❌ 不主动启动 Phase D-H
- ❌ 不修改 cognee 索引
- ❌ 不重写主报告 `项目深度调研分析报告.md`

---

## 八、验证步骤

### 8.1 P0 验证

1. `Read c:\Users\20564\.trae-cn\memory\projects\-e-Development-MyAwesomeApp-PomodoroXII\project_memory.md` 应存在
2. 内容包含 "Phase C 实际进度 = 0%" 和 "测试总数 = 214"

### 8.2 P1 验证

```powershell
cd 'e:\Development\MyAwesomeApp\PomodoroXII\backend'
.venv\Scripts\python.exe -m pytest tests/test_routes_v1.py -k paginated -v
.venv\Scripts\python.exe -m pytest tests/test_sync_indexes.py -v
.venv\Scripts\python.exe -m pytest tests/test_base_service.py -k "version or updated_at" -v
.venv\Scripts\python.exe -m pytest -q
```

### 8.3 P2 验证（Phase C）

```powershell
.venv\Scripts\python.exe -m pytest tests/test_sync_safety.py -v
.venv\Scripts\python.exe -m pytest tests/test_sync_service.py -v
.venv\Scripts\python.exe -m pytest tests/test_note_service.py -k savepoint -v
.venv\Scripts\python.exe -m pytest tests/test_sync_routes.py -v
.venv\Scripts\python.exe -m pytest tests/test_sync_integration.py -v
.venv\Scripts\python.exe -m pytest -q  # 预期 ~289 测试
```

### 8.4 P3 验证

```powershell
.venv\Scripts\python.exe -m pytest tests/test_task_service.py -k update_tags -v
.venv\Scripts\python.exe -m pytest tests/test_routes_v1.py -k purge -v
.venv\Scripts\python.exe -m pytest tests/test_serializers.py -v
.venv\Scripts\python.exe -m pytest tests/test_models.py -k "note_status or task_index" -v
.venv\Scripts\python.exe -m pytest tests/test_deps.py -v
```

---

## 九、关键约束（铁律）

1. **三层铁律**: Routers commit / Services flush / Models 纯数据 — 严格遵守
2. **TDD 流程**: 每个任务先写测试（Red），再实现（Green），最后重构（Refactor）
3. **代码引用规范**: 报告中所有代码引用使用 `file:///` 链接格式
4. **不创建非必要文件**: 仅创建计划要求的代码文件 + 测试文件 + 1 个 Alembic migration
5. **路径规范**: 当前仓库为 `e:\Development\MyAwesomeApp\PomodoroXII\backend`
6. **Skill 使用**: test-driven-development 用于 TDD 流程，agent-browser 用于端到端测试（可选）
7. **不动用户代码之外的范围**: 不修改 cognee 索引，不修改 session_memory，不修改 user_profile

---

## 十、风险与缓解

| 风险 | 严重度 | 缓解措施 |
|------|--------|---------|
| Phase C 旧计划代码与当前仓库不兼容 | 中 | 逐任务适配路径，先验证 import 路径 |
| TDD 流程耗时较长 | 中 | 严格遵守计划顺序，每任务完成后立即验证 |
| sync_mode 集成可能破坏现有 NoteService | 高 | 添加 sync_mode=False 默认值，向后兼容 |
| Alembic migration 可能与 P2-5 冲突 | 低 | 在 P1 阶段先创建 migration |
| agent-browser Skill 不可用 | 低 | C10 集成测试使用 client fixture 替代 |
| 测试总数增长后 conftest 隔离可能失效 | 中 | 每阶段全量回归验证 |

---

## 十一、执行入口

按本计划执行，从 P0 (任务 1) 开始。每完成一个阶段运行 pytest 全量回归验证。详细任务代码模板见 [优先级修复与PhaseC实施计划.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/.trae/documents/优先级修复与PhaseC实施计划.md)。
