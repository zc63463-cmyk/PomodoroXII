# 扩展性 4.5 星提升 — TDD 实施计划 v2

> **源规划**: [核心文档/14-扩展性4.5星提升规划.md](E:/Development/MyAwesomeApp/PomodoroXII/核心文档/14-扩展性4.5星提升规划.md)
> **执行模式**: TDD Red-Green-Refactor + 顺序 Phase 推进
> **基线**: 361 tests passing（Phase D 完成后）
> **本计划替代**: `.trae/documents/扩展性4.5星提升-实施计划.md`（v1，未启动）

## 摘要

把规划文档的策略转化为可直接执行的战术任务。核心矛盾：`REGISTRY` 单例（20 实体，snake_case）已存在但 3 个主要消费方（`SyncService.ENTITY_REGISTRY`、`trash._ENTITY_MAP`、`StatsService`）都没消费它，导致事实源分裂。

通过 4 个 Phase 逐步收敛，每个 Phase 严格遵循 TDD：先写**一个**失败测试，观察失败原因正确，再写最小实现。

**执行策略**：顺序执行（Phase 1 → 2 → 3 → 4），原因：
1. Phase 1 的 parity gate 是 Phase 2-4 自动化的前提
2. 多 Phase 共享文件（`entities.py`、`builtin.py`、`conftest.py`），并行会冲突
3. TDD 要求每个测试单独观察失败

## 当前状态分析

### 已确认的事实源分裂（3 处）

| 位置 | 类型 | 实体数 | 键名风格 | 问题 |
|---|---|---|---|---|
| [app/services/sync.py:51-66](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/sync.py#L51-L66) `ENTITY_REGISTRY` | `dict[str, dict]` | 14 | camelCase | 独立维护，不消费 REGISTRY |
| [app/routes/v1/trash.py:37-41](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/trash.py#L37-L41) `_ENTITY_MAP` | `dict[str, type]` | 3 | snake_case | 硬编码，不消费 `REGISTRY.list_soft_delete()` |
| [app/services/stats.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/stats.py) | 7 个方法 | 7 端点 | N/A | 完全无 registry 概念，REST 与 MCP 已漂移 |

### REGISTRY 已具备的能力（[app/registry/__init__.py:34-83](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/__init__.py#L34-L83)）

- `list_sync_enabled()` 返回 14 个业务实体 ✅
- `list_soft_delete()` 返回 `{note, folder, quick_note}` ✅
- `EntitySpec` 含 `model_path`（字符串）、`table_name`、`storage_type`、`fields` ✅
- 但**缺少**：`sync_entity_type`、`pull_key`、`delete_strategy`、`route_prefix`、`service_path`、`schema_module`、`schema_prefix`
- **没有** `resolve_model()` 工具函数（`model_path` 字符串从未被动态 import）

### 键名风格冲突（关键阻碍）

- `REGISTRY` 用 snake_case：`quick_note`、`habit_check_in`、`time_block`
- `ENTITY_REGISTRY` 用 camelCase：`quickNote`、`habitCheckIn`、`timeBlock`
- **必须保持 camelCase 协议兼容**（客户端历史协议）

### MCP 环境状态

- `pyproject.toml:20` 已声明 `fastmcp>=2.0` ✅
- `tests/test_mcp_server.py` 存在，含 13 个测试 ✅
- **已知阻碍**：Trae IDE 沙箱阻止 Windows `O_TEMPORARY` flag 文件创建，pytest capture 插件失败
- **绕过方案**：所有 pytest 命令统一加 `--capture=no -p no:cacheprovider -p no:tmpdir`

### REST/MCP Stats 漂移（已确认）

| REST 端点 | MCP 工具 | 状态 |
|---|---|---|
| `/overview` | `get_stats_overview` | ✅ 一致 |
| `/focus-trend` | `get_focus_trend` | ✅ 一致 |
| `/task-distribution` | `get_task_distribution` | ✅ 一致 |
| `/daily-detail` | **无** | ❌ 漂移 |
| `/habit-summary` | `get_habit_summary` | ✅ 一致 |
| `/schedule-summary` | `get_schedule_summary` | ✅ 一致 |
| `/note-summary` | `get_note_summary` | ✅ 一致 |

## 假设与决策

### 核心决策

1. **键名兼容**：`EntitySpec` 新增 `sync_entity_type` 字段（camelCase），不填则 fallback 到 `name`。所有 14 个 sync 实体显式声明 camelCase 名称，保证协议不变。
2. **pull_key 显式**：不依赖复数化规则（避免 `memoComment` → `memoComments` 误伤），所有实体显式声明。
3. **model_path 动态解析**：新增 `app/registry/resolve.py` 提供 `resolve_model(spec_or_path)` 和 `import_all_models()`，用 `importlib`。
4. **trash 不破坏 FS**：`file_system/engine/trash_ops.py` 独立于 DB trash，本计划不动 FS 层。
5. **特殊实体豁免**：Note（FS+DB split）、Folder（cascade）、Relation（junctions）在 parity test 中显式声明例外。
6. **脚手架生成显式代码**：不做运行时黑盒 CRUD，`scaffold_entity.py` 生成可读 Python 文件。
7. **Stats 不强行抽象**：Phase 4 用 `StatSpec` 登记 + parity test，不动态生成路由（保留显式代码）。
8. **MCP 测试环境**：使用 `--capture=no -p no:cacheprovider -p no:tmpdir` 绕过 Trae 沙箱 tempfile 限制。

### TDD 纪律

- 每个新功能先写**一个**失败测试，观察失败原因正确，再写最小实现
- 不批量写测试再实现（违反 TDD）
- 重构阶段保持测试绿
- 不保留"参考代码"——探索性代码 TDD 前必须删除

### 通用 pytest 命令

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
uv run pytest tests/<test_file>.py -v --capture=no -p no:cacheprovider -p no:tmpdir
```

如 `--capture=no -p no:cacheprovider` 仍因 tempfile 失败，则改为：
```powershell
uv run python -m pytest tests/<test_file>.py -v -s --no-header --capture=no
```

## Phase 1: 一致性门禁优先

**目标**：让遗漏暴露，不急着自动生成。本 Phase 几乎不改生产代码，只加测试 + 例外文档 + 1 个 `resolve_model()` 工具 + 1 个 MCP 工具补齐。

### P1.1 跳过 MCP 环境诊断（沙箱限制）

**决策**：跳过 v1 计划中的 P1.1 MCP 环境诊断步骤。原因：
- Trae IDE 沙箱阻止 Windows `O_TEMPORARY` flag 文件创建，pytest 无法启动
- 这是环境问题，非代码问题
- 后续 P1.6 的 MCP parity test 会顺带验证 MCP 是否可运行
- 如 P1.6 也失败，再回头处理环境问题

### P1.2 Registry vs Sync parity test

**RED** — 新建 `tests/test_parity_registry_sync.py`：

```python
"""Parity gate: REGISTRY.list_sync_enabled() vs SyncService.ENTITY_REGISTRY.

确保两个事实源在实体数量、entity_type、pull_key 上完全一致。
新增 sync 实体时必须同时更新两处（或 Phase 2 后只更新 REGISTRY）。
"""
from __future__ import annotations

from app.registry import REGISTRY
from app.services.sync import ENTITY_REGISTRY


def test_sync_registry_count_matches_registry_sync_enabled():
    """ENTITY_REGISTRY 数量必须等于 REGISTRY.list_sync_enabled() 数量。"""
    expected = len(REGISTRY.list_sync_enabled())
    actual = len(ENTITY_REGISTRY)
    assert actual == expected, (
        f"Sync ENTITY_REGISTRY has {actual} entries, "
        f"but REGISTRY.list_sync_enabled() returns {expected}. "
        "New sync entity must be added to both."
    )


def test_sync_registry_entity_types_match():
    """每个 ENTITY_REGISTRY key 必须对应一个 REGISTRY spec 的 sync_entity_type/name。"""
    sync_names_in_registry = {
        spec.sync_entity_type or spec.name
        for spec in REGISTRY.list_sync_enabled()
    }
    entity_registry_keys = set(ENTITY_REGISTRY.keys())
    missing = entity_registry_keys - sync_names_in_registry
    extra = sync_names_in_registry - entity_registry_keys
    assert not missing, f"In ENTITY_REGISTRY but not REGISTRY: {missing}"
    assert not extra, f"In REGISTRY but not ENTITY_REGISTRY: {extra}"


def test_sync_registry_pull_keys_match():
    """每个 ENTITY_REGISTRY pull_key 必须与 REGISTRY spec 的 pull_key 一致。

    注意：此测试在 Phase 2 EntitySpec 扩展前会失败，因为 REGISTRY spec
    当前没有 pull_key 字段。Phase 1 先用 skip 标记，Phase 2 启用。
    """
    import pytest
    pytest.skip("Phase 2 task: EntitySpec needs pull_key field first")
```

**Verify RED**:
```powershell
uv run pytest tests/test_parity_registry_sync.py -v --capture=no -p no:cacheprovider
```

预期：前 2 个测试 FAIL（因为 `EntitySpec` 没有 `sync_entity_type` 属性），第 3 个 SKIP。

**GREEN**：先在 `entities.py` 加 `sync_entity_type`/`pull_key` 字段（带默认值），让前 2 个测试通过：

```python
# app/registry/entities.py — 在 EntitySpec 末尾追加
sync_entity_type: str | None = None  # camelCase for sync protocol
pull_key: str | None = None
```

**Verify GREEN**：前 2 个测试 PASS，第 3 个 SKIP。注意：此时尚未在 `builtin.py` 中填值，因为 `sync_entity_type or name` fallback 让默认行为不变。

### P1.3 Registry vs ORM model parity test + resolve_model()

**RED** — 新建 `tests/test_parity_registry_orm.py`：

```python
"""Parity gate: REGISTRY.model_path vs actual ORM model classes.

确保每个 spec.model_path 都能 import 到一个 ORM 类，且该类的
__tablename__ 与 spec.table_name 一致。
"""
from __future__ import annotations

import pytest

from app.registry import REGISTRY
from app.registry.resolve import resolve_model


@pytest.mark.parametrize("spec_name", [s.name for s in REGISTRY.list()])
def test_model_path_resolves_to_valid_orm_class(spec_name):
    """每个 spec.model_path 必须 import 到有效的 ORM 类。"""
    spec = REGISTRY.get(spec_name)
    model = resolve_model(spec)
    assert model is not None, f"Cannot resolve model_path: {spec.model_path}"


@pytest.mark.parametrize("spec_name", [s.name for s in REGISTRY.list()])
def test_model_tablename_matches_spec(spec_name):
    """ORM __tablename__ 必须与 spec.table_name 一致。"""
    spec = REGISTRY.get(spec_name)
    model = resolve_model(spec)
    assert model.__tablename__ == spec.table_name, (
        f"{spec.name}: ORM __tablename__={model.__tablename__!r}, "
        f"spec.table_name={spec.table_name!r}"
    )
```

**Verify RED**：测试 FAIL，因为 `app.registry.resolve` 模块不存在。

**GREEN** — 新建 `app/registry/resolve.py`：

```python
"""Dynamic ORM model resolution from EntitySpec.model_path.

提供两个函数：
- resolve_model(spec_or_path): 从 model_path 字符串动态 import ORM 类
- import_all_models(): 遍历 REGISTRY 调用 resolve_model，确保所有模型注册到 Base.metadata
"""
from __future__ import annotations

import importlib
from typing import Union

from app.registry import REGISTRY, EntitySpec


def resolve_model(spec_or_path: Union[EntitySpec, str]) -> type:
    """从 EntitySpec.model_path 或字符串路径 import ORM 类。

    Args:
        spec_or_path: EntitySpec 实例或 "app.models.task.Task" 形式字符串

    Returns:
        ORM 类（如 Task）

    Raises:
        ImportError: 模块无法 import
        AttributeError: 模块中找不到指定类
    """
    path = spec_or_path.model_path if isinstance(spec_or_path, EntitySpec) else spec_or_path
    module_path, _, class_name = path.rpartition(".")
    if not module_path:
        raise ValueError(f"Invalid model_path: {path!r}")
    module = importlib.import_module(module_path)
    return getattr(module, class_name)


def import_all_models() -> None:
    """遍历 REGISTRY 调用 resolve_model，确保所有 ORM 类被 import。

    用于 Alembic env.py 和 space_manager._init_schema() 前，
    确保 Base.metadata 包含所有表。
    """
    for spec in REGISTRY.list():
        resolve_model(spec)
```

**Verify GREEN**：所有测试 PASS。

### P1.4 Registry vs Schema parity test

**RED** — 新建 `tests/test_parity_registry_schemas.py`：

```python
"""Parity gate: REGISTRY vs Pydantic schemas.

确保每个 BUSINESS 实体在 app.schemas 中有对应的 Create/Update/Response schema。
特殊实体（Note content、Folder cascade）在例外表中声明。
"""
from __future__ import annotations

import importlib

import pytest

from app.registry import REGISTRY
from app.registry.entities import EntityCategory


# 例外表：实体 -> 例外原因
SCHEMA_EXCEPTIONS: dict[str, str] = {
    # Junction tables 没有独立 schema（通过父实体管理）
    "session_quick_note": "Junction table, no independent schema",
    "schedule_quick_note": "Junction table, no independent schema",
    "task_quick_note": "Junction table, no independent schema",
}


@pytest.mark.parametrize("spec_name", [
    s.name for s in REGISTRY.list_by_category(EntityCategory.BUSINESS)
    if s.name not in SCHEMA_EXCEPTIONS
])
def test_business_entity_has_schema_module(spec_name):
    """每个 BUSINESS 实体必须有 app.schemas.<name> 模块。"""
    module_path = f"app.schemas.{spec_name}"
    try:
        module = importlib.import_module(module_path)
    except ImportError as exc:
        pytest.fail(f"Missing schema module {module_path}: {exc}")
    assert module is not None


@pytest.mark.parametrize("spec_name", [
    s.name for s in REGISTRY.list_by_category(EntityCategory.BUSINESS)
    if s.name not in SCHEMA_EXCEPTIONS
])
def test_business_entity_has_response_schema(spec_name):
    """每个 BUSINESS 实体必须有 <Name>Response schema 类。"""
    module = importlib.import_module(f"app.schemas.{spec_name}")
    # Convert snake_case to PascalCase: quick_note -> QuickNote
    class_name = "".join(p.capitalize() for p in spec_name.split("_")) + "Response"
    assert hasattr(module, class_name), (
        f"{module.__name__} missing {class_name}"
    )
```

**Verify RED**：测试应 PASS（schemas 已存在），因为这只是 gate。如果失败，说明有遗漏的 schema，需补齐。

**注**：此测试是"已通过"测试，作用是 gate。新增实体时若遗漏 schema，此测试会失败。

### P1.5 Registry vs Routes parity test

**RED** — 新建 `tests/test_parity_routes.py`：

```python
"""Parity gate: REGISTRY vs routes/v1/__init__.py include list.

确保 route_enabled=True 的实体在 build_v1_router() 中有对应 include_router。
"""
from __future__ import annotations

import inspect

from app.registry import REGISTRY
from app.routes.v1 import build_v1_router


def test_build_v1_router_includes_all_expected_modules():
    """build_v1_router 必须包含 16 个 sub-routers（3 meta + 13 space）。"""
    router = build_v1_router()
    # 每个 include_router 产生一个 route前缀
    # 通过检查 router.routes 的 prefix 来判断
    # 这里用更简单的方法：检查源代码中的 include_router 调用次数
    source = inspect.getsource(build_v1_router)
    include_count = source.count("router.include_router(")
    assert include_count == 16, (
        f"Expected 16 include_router calls, got {include_count}"
    )


def test_expected_route_prefixes_present():
    """关键路由前缀必须存在。"""
    router = build_v1_router()
    # 通过 OpenAPI 收集所有路径前缀
    # 这里用更简单的方法：直接检查源代码字符串
    source = inspect.getsource(build_v1_router)
    expected_prefixes = [
        'prefix="/tasks"',
        'prefix="/sessions"',
        'prefix="/notes"',
        'prefix="/folders"',
        'prefix="/quick-notes"',
        'prefix="/reflections"',
        'prefix="/habits"',
        'prefix="/schedules"',
        'prefix="/time-blocks"',
        'prefix="/trash"',
        'prefix="/stats"',
        'prefix="/settings"',
        'prefix="/sync"',
        'prefix="/auth"',
        'prefix="/spaces"',
        'prefix="/meta"',
    ]
    missing = [p for p in expected_prefixes if p not in source]
    assert not missing, f"Missing route prefixes: {missing}"
```

**Verify RED/GREEN**：此测试应直接 PASS（gate test）。

### P1.6 Stats REST vs MCP parity test + 补 daily_detail MCP 工具

**RED** — 新建 `tests/test_parity_stats_mcp.py`：

```python
"""Parity gate: REST /stats 端点 vs MCP stats 工具。

确保每个 REST stats 端点有对应 MCP 工具（或显式豁免）。
"""
from __future__ import annotations

from app.mcp.server import mcp


# REST endpoint -> 期望的 MCP 工具名
EXPECTED_MAPPING: dict[str, str] = {
    "/overview": "get_stats_overview",
    "/focus-trend": "get_focus_trend",
    "/task-distribution": "get_task_distribution",
    "/daily-detail": "get_daily_detail",  # 当前缺失，本测试会失败
    "/habit-summary": "get_habit_summary",
    "/schedule-summary": "get_schedule_summary",
    "/note-summary": "get_note_summary",
}


def test_mcp_has_all_stats_tools():
    """MCP server 必须注册所有 stats 工具。"""
    # FastMCP 暴露工具的方式：mcp._tool_manager._tools 或类似
    # 具体属性名依 fastmcp 版本，需运行时探测
    tools = mcp._tool_manager._tools if hasattr(mcp, "_tool_manager") else {}
    tool_names = set(tools.keys()) if tools else set()
    if not tool_names:
        # FastMCP 2.x: 用 list_tools() 异步方法
        # 但同步测试中，我们检查 mcp.__dict__ 中带 @mcp.tool 装饰的函数
        import app.mcp.server as mcp_module
        tool_names = {
            name for name, obj in inspect.getmembers(mcp_module)
            if callable(obj) and getattr(obj, "_mcp_tool", False)
        }
    missing = set(EXPECTED_MAPPING.values()) - tool_names
    assert not missing, f"MCP missing stats tools: {missing}"


def test_daily_detail_mcp_tool_exists():
    """daily_detail 必须有对应 MCP 工具（或显式豁免）。"""
    import inspect
    import app.mcp.server as mcp_module
    assert hasattr(mcp_module, "get_daily_detail"), (
        "MCP server missing get_daily_detail tool (REST /daily-detail has no MCP equivalent)"
    )
```

**Verify RED**：`test_daily_detail_mcp_tool_exists` FAIL，因为 `get_daily_detail` 不存在。

**GREEN** — 在 [app/mcp/server.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/mcp/server.py) 添加：

```python
@mcp.tool
async def get_daily_detail(space_id: str, date: str) -> dict:
    """Get session count and total duration for a specific date.

    Args:
        space_id: The space to query.
        date: Date in YYYY-MM-DD format.

    Returns:
        Dict with date, count, duration.
    """
    from app.services.stats import StatsService

    async with get_space_session(space_id) as db:
        return await StatsService(db).daily_detail(date=date)
```

插入位置：`get_task_distribution` 之后，`get_habit_summary` 之前。

**Verify GREEN**：测试 PASS。同时新增一个测试验证 `get_daily_detail` 可执行：

```python
@pytest.mark.asyncio
async def test_get_daily_detail_returns_dict(space_session):
    """get_daily_detail should return date/count/duration dict."""
    from app.mcp.server import get_daily_detail

    result = await get_daily_detail("spc_test", "2026-07-04")
    assert isinstance(result, dict)
    assert "date" in result or "count" in result
```

### P1.7 Alembic metadata parity test

**RED** — 新建 `tests/test_parity_alembic_metadata.py`：

```python
"""Parity gate: Alembic head 生成的表/列 vs ORM Base.metadata。

确保 Alembic 迁移链覆盖所有 ORM 模型声明的表。
"""
from __future__ import annotations

from pathlib import Path

import pytest
from alembic import command
from alembic.config import Config
from sqlalchemy import create_engine, inspect


def _alembic_cfg() -> Config:
    backend_dir = Path(__file__).resolve().parent.parent
    ini_path = backend_dir / "alembic.ini"
    cfg = Config(str(ini_path))
    cfg.set_main_option("script_location", str(backend_dir / "alembic"))
    cfg.config_file_name = None
    return cfg


def test_alembic_head_tables_match_metadata(tmp_path: Path):
    """Alembic upgrade head 生成的表必须等于 Base.metadata 表集合。"""
    from app.db.base import Base
    from app.models import *  # noqa: F401, F403 - register all models
    from app.db.models import meta  # noqa: F401

    cfg = _alembic_cfg()
    db_path = tmp_path / "parity_test.db"
    engine = create_engine(f"sqlite:///{db_path.as_posix()}")
    with engine.begin() as conn:
        cfg.attributes["connection"] = conn
        command.upgrade(cfg, "head")

    alembic_tables = set(inspect(engine).get_table_names()) - {"alembic_version"}
    metadata_tables = set(Base.metadata.tables.keys())
    engine.dispose()

    missing = metadata_tables - alembic_tables
    extra = alembic_tables - metadata_tables
    assert not missing, f"In metadata but not Alembic: {missing}"
    assert not extra, f"In Alembic but not metadata: {extra}"
```

**Verify RED/GREEN**：测试应直接 PASS（gate test）。如果失败，说明 Alembic 迁移链有遗漏。

### P1.8 Parity 例外文档

**新建** `核心文档/15-扩展性parity例外表.md`：

```markdown
# 扩展性 parity 例外表

> **目的**：记录所有 parity test 中允许的例外，避免自动化误伤特殊实体。
> **维护**：新增例外时必须在此文档登记，并在对应 test 中声明。

## Schema 例外

| 实体 | 例外原因 | 豁免内容 |
|---|---|---|
| `session_quick_note` | Junction table | 无独立 Pydantic schema |
| `schedule_quick_note` | Junction table | 无独立 Pydantic schema |
| `task_quick_note` | Junction table | 无独立 Pydantic schema |
| `note` | FS+DB split | `content` 字段不在 ORM columns（在 FS） |
| `folder` | Cascade soft-delete | `parent_id` 自引用需 cascade 处理 |

## Sync 例外

| 实体 | 例外原因 | 豁免内容 |
|---|---|---|
| `note` | FS+DB split | push 走 `_push_note_event` → NoteService(sync_mode=True) |
| `folder` | Cascade | push update 需 `check_folder_circular_ref` hook |

## Trash 例外

| 实体 | 例外原因 | 豁免内容 |
|---|---|---|
| `folder` | Cascade | purge 时需级联删除 descendants |
| `note` | FS+DB split | delete 时需同步删除 FS .md 文件 |

## Stats 例外

| 端点 | 例外原因 | 豁免内容 |
|---|---|---|
| 无 | 所有 stats 端点必须有对应 MCP 工具 | 无豁免 |
```

### Phase 1 验收

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
uv run pytest tests/test_parity_registry_sync.py tests/test_parity_registry_orm.py tests/test_parity_registry_schemas.py tests/test_parity_routes.py tests/test_parity_stats_mcp.py tests/test_parity_alembic_metadata.py tests/test_mcp_server.py tests/test_registry.py -v --capture=no -p no:cacheprovider
```

**通过标准**：
- 6 个 parity test 文件全部 PASS（除 P1.2 第 3 个测试 SKIP）
- 现有 361 测试无回归
- `get_daily_detail` MCP 工具可执行

## Phase 2: EntitySpec 扩展 + registry 派生 sync/trash

**目标**：消除最危险的事实源分裂。让 `SyncService.ENTITY_REGISTRY` 和 `trash._ENTITY_MAP` 都从 `REGISTRY` 派生。

### P2.1 EntitySpec 扩展 7 个字段

**RED** — 新建 `tests/test_entity_spec_extension.py`：

```python
"""EntitySpec 扩展字段测试。"""
from __future__ import annotations

from app.registry import REGISTRY
from app.registry.entities import EntitySpec, EntityCategory, StorageType, FieldSpec


def test_entity_spec_has_sync_entity_type_field():
    """EntitySpec 必须有 sync_entity_type 字段。"""
    spec = EntitySpec(
        name="test",
        model_path="app.models.task.Task",
        table_name="tests",
        storage_type=StorageType.DB_ONLY,
        category=EntityCategory.BUSINESS,
        sync_enabled=True,
        soft_delete=False,
        fields=(FieldSpec("id", "string", nullable=False),),
        sync_entity_type="testEntity",
    )
    assert spec.sync_entity_type == "testEntity"


def test_entity_spec_has_pull_key_field():
    """EntitySpec 必须有 pull_key 字段。"""
    spec = EntitySpec(
        name="test",
        model_path="app.models.task.Task",
        table_name="tests",
        storage_type=StorageType.DB_ONLY,
        category=EntityCategory.BUSINESS,
        sync_enabled=True,
        soft_delete=False,
        fields=(FieldSpec("id", "string", nullable=False),),
        pull_key="tests",
    )
    assert spec.pull_key == "tests"


def test_entity_spec_has_delete_strategy_field():
    """EntitySpec 必须有 delete_strategy 字段，默认 'hard_tombstone'。"""
    spec = EntitySpec(
        name="test",
        model_path="app.models.task.Task",
        table_name="tests",
        storage_type=StorageType.DB_ONLY,
        category=EntityCategory.BUSINESS,
        sync_enabled=True,
        soft_delete=False,
        fields=(FieldSpec("id", "string", nullable=False),),
    )
    assert spec.delete_strategy == "hard_tombstone"


def test_entity_spec_effective_sync_entity_type_fallback():
    """sync_entity_type 不填时，effective_sync_entity_type fallback 到 name。"""
    spec = EntitySpec(
        name="task",
        model_path="app.models.task.Task",
        table_name="tasks",
        storage_type=StorageType.DB_ONLY,
        category=EntityCategory.BUSINESS,
        sync_enabled=True,
        soft_delete=False,
        fields=(FieldSpec("id", "string", nullable=False),),
    )
    assert spec.effective_sync_entity_type == "task"


def test_entity_spec_effective_pull_key_fallback():
    """pull_key 不填时，effective_pull_key fallback 到 name + 's'。"""
    spec = EntitySpec(
        name="task",
        model_path="app.models.task.Task",
        table_name="tasks",
        storage_type=StorageType.DB_ONLY,
        category=EntityCategory.BUSINESS,
        sync_enabled=True,
        soft_delete=False,
        fields=(FieldSpec("id", "string", nullable=False),),
    )
    assert spec.effective_pull_key == "tasks"
```

**Verify RED**：测试 FAIL，因为 `EntitySpec` 没有 `sync_entity_type`/`pull_key`/`delete_strategy` 字段和 `effective_*` 属性。

**GREEN** — 修改 [app/registry/entities.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/entities.py)：

```python
@dataclass(frozen=True)
class EntitySpec:
    # ... 现有字段 ...
    primary_key: str = "id"
    description: str = ""

    # 新增字段
    sync_entity_type: str | None = None
    pull_key: str | None = None
    route_prefix: str | None = None
    service_path: str | None = None
    schema_module: str | None = None
    schema_prefix: str | None = None
    delete_strategy: str = "hard_tombstone"
    route_enabled: bool = False
    mcp_schema_enabled: bool = True

    @property
    def field_names(self) -> tuple[str, ...]:
        return tuple(f.name for f in self.fields)

    @property
    def effective_sync_entity_type(self) -> str:
        """sync_entity_type or fallback to name."""
        return self.sync_entity_type or self.name

    @property
    def effective_pull_key(self) -> str:
        """pull_key or fallback to name + 's' (simple plural)."""
        return self.pull_key or (self.name + "s")
```

**Verify GREEN**：测试 PASS。

### P2.2 为 14 个 sync 实体声明 camelCase

**修改** [app/registry/builtin.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/builtin.py)：

为 14 个 BUSINESS 实体追加 `sync_entity_type` 和 `pull_key` 显式声明。映射表：

| name | sync_entity_type | pull_key |
|---|---|---|
| task | task | tasks |
| session | session | sessions |
| note | note | notes |
| folder | folder | folders |
| quick_note | quickNote | quickNotes |
| reflection | reflection | reflections |
| habit | habit | habits |
| habit_check_in | habitCheckIn | habitCheckIns |
| schedule | schedule | schedules |
| time_block | timeBlock | timeBlocks |
| memo_comment | memoComment | memoComments |
| session_quick_note | sessionQuickNote | sessionQuickNotes |
| schedule_quick_note | scheduleQuickNote | scheduleQuickNotes |
| task_quick_note | taskQuickNote | taskQuickNotes |

同时为 soft_delete 实体声明 `delete_strategy`：
- `note`: `delete_strategy="fs_saga"`
- `folder`: `delete_strategy="cascade_soft_delete"`
- `quick_note`: `delete_strategy="soft_delete"`

示例（quick_note 修改前后对比）：

修改前：
```python
REGISTRY.register(EntitySpec(
    name="quick_note",
    ...
    soft_delete=True,
    fields=...,
    description="...",
))
```

修改后：
```python
REGISTRY.register(EntitySpec(
    name="quick_note",
    ...
    soft_delete=True,
    fields=...,
    sync_entity_type="quickNote",
    pull_key="quickNotes",
    delete_strategy="soft_delete",
    description="...",
))
```

**验收**：现有 361 测试无回归 + P1.2 第 3 个测试可启用（去掉 `pytest.skip`）。

### P2.3 实现 build_sync_registry() + 测试

**RED** — 新建 `tests/test_build_sync_registry.py`：

```python
"""build_sync_registry() 派生测试。"""
from __future__ import annotations

from app.registry.sync_registry import build_sync_registry


def test_build_sync_registry_returns_14_entries():
    """build_sync_registry 必须返回 14 个实体。"""
    registry = build_sync_registry()
    assert len(registry) == 14


def test_build_sync_registry_uses_camelcase_keys():
    """build_sync_registry 的 key 必须是 camelCase。"""
    registry = build_sync_registry()
    assert "quickNote" in registry
    assert "habitCheckIn" in registry
    assert "timeBlock" in registry
    assert "memoComment" in registry


def test_build_sync_registry_includes_model_and_pull_key():
    """每个 entry 必须含 model 和 pull_key。"""
    registry = build_sync_registry()
    for entity_type, entry in registry.items():
        assert "model" in entry, f"{entity_type} missing 'model'"
        assert "pull_key" in entry, f"{entity_type} missing 'pull_key'"
        assert entry["model"] is not None
        assert isinstance(entry["pull_key"], str)


def test_build_sync_registry_pull_keys_match_legacy():
    """build_sync_registry 的 pull_key 必须与原 ENTITY_REGISTRY 一致。"""
    from app.services.sync import ENTITY_REGISTRY
    registry = build_sync_registry()
    for key, entry in registry.items():
        assert entry["pull_key"] == ENTITY_REGISTRY[key]["pull_key"], (
            f"{key}: build_sync_registry pull_key={entry['pull_key']!r}, "
            f"legacy={ENTITY_REGISTRY[key]['pull_key']!r}"
        )


def test_build_sync_registry_models_match_legacy():
    """build_sync_registry 的 model 必须与原 ENTITY_REGISTRY 一致。"""
    from app.services.sync import ENTITY_REGISTRY
    registry = build_sync_registry()
    for key, entry in registry.items():
        assert entry["model"] is ENTITY_REGISTRY[key]["model"], (
            f"{key}: model mismatch"
        )
```

**Verify RED**：测试 FAIL，因为 `app.registry.sync_registry` 模块不存在。

**GREEN** — 新建 `app/registry/sync_registry.py`：

```python
"""SyncService registry derived from REGISTRY.

替代 services/sync.py 中手工维护的 ENTITY_REGISTRY。
"""
from __future__ import annotations

from typing import Any

from app.registry import REGISTRY
from app.registry.resolve import resolve_model


def build_sync_registry() -> dict[str, dict[str, Any]]:
    """从 REGISTRY.list_sync_enabled() 派生 sync registry。

    Returns:
        dict[entity_type_camelCase, {"model": ORMClass, "pull_key": str, "spec": EntitySpec}]

    保证：
    - key 用 spec.effective_sync_entity_type（camelCase 兼容历史协议）
    - model 用 resolve_model(spec) 动态 import
    - pull_key 用 spec.effective_pull_key
    """
    result: dict[str, dict[str, Any]] = {}
    for spec in REGISTRY.list_sync_enabled():
        entity_type = spec.effective_sync_entity_type
        model = resolve_model(spec)
        pull_key = spec.effective_pull_key
        result[entity_type] = {
            "model": model,
            "pull_key": pull_key,
            "spec": spec,
        }
    return result
```

**Verify GREEN**：测试 PASS。

### P2.4 SyncService 切换到派生 registry

**修改** [app/services/sync.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/sync.py)：

替换 lines 19-33 的硬编码 ORM imports 和 lines 51-66 的 `ENTITY_REGISTRY` dict：

```python
# 删除硬编码 import：
# from app.models.folder import Folder
# ... (14 行)

# 改为：
from app.registry.sync_registry import build_sync_registry

# 替换 ENTITY_REGISTRY dict：
ENTITY_REGISTRY: dict[str, dict[str, Any]] = build_sync_registry()
```

**注意**：保留 `from app.models.note import Note` 和 `from app.models.tombstone import Tombstone`，因为它们在 `_push_note_event` 和 `_fetch_tombstones` 中直接使用。

**Verify**：现有 `test_sync_service.py` / `test_sync_integration.py` / `test_sync_routes.py` 全部 PASS（共 ~30 个 sync 测试）。这是关键回归点。

### P2.5 Trash registry 化

**修改** [app/routes/v1/trash.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/trash.py)：

替换 lines 23-49 的硬编码：

```python
# 删除：
# from app.models.folder import Folder
# from app.models.note import Note
# from app.models.quick_note import QuickNote
# _ENTITY_MAP: dict[str, type] = {
#     "note": Note,
#     "folder": Folder,
#     "quick_note": QuickNote,
# }
# def _resolve_model(entity_type: str) -> type: ...

# 改为：
from app.registry import REGISTRY
from app.registry.resolve import resolve_model

# 派生 soft-delete 实体映射
_ENTITY_MAP: dict[str, type] = {
    spec.name: resolve_model(spec)
    for spec in REGISTRY.list_soft_delete()
}


def _resolve_model(entity_type: str) -> type:
    """Return the ORM model class for *entity_type* or raise ValidationError."""
    model = _ENTITY_MAP.get(entity_type)
    if model is None:
        raise ValidationError(f"Unknown entity type: {entity_type!r}")
    return model
```

**注意**：`list_trash` 端点中针对 `Note`/`Folder`/`QuickNote` 的硬编码 SELECT 仍保留（因为每种的 `title` 提取逻辑不同）。后续可抽 `TrashService`，但本 Phase 不做（YAGNI）。

**Verify**：`test_trash_routes.py` 全部 PASS。

### P2.6 启用 P1.2 第 3 个测试

**修改** `tests/test_parity_registry_sync.py`：删除 `test_sync_registry_pull_keys_match` 中的 `pytest.skip`。

**Verify**：测试 PASS。

### Phase 2 验收

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
uv run pytest tests/test_parity_*.py tests/test_build_sync_registry.py tests/test_entity_spec_extension.py tests/test_sync_service.py tests/test_sync_integration.py tests/test_sync_routes.py tests/test_trash_routes.py tests/test_registry.py tests/test_mcp_server.py -v --capture=no -p no:cacheprovider
```

**通过标准**：
- 所有新测试 PASS
- 现有 361 测试无回归
- `SyncService.ENTITY_REGISTRY` 内部由 `build_sync_registry()` 派生
- `trash._ENTITY_MAP` 由 `REGISTRY.list_soft_delete()` 派生

## Phase 3: 脚手架与迁移流程

**目标**：让新实体/字段扩展成为可重复流程。

### P3.1 scaffold_entity.py 脚手架

**新建** `backend/scripts/scaffold_entity.py`：

CLI 工具，输入实体名/类名/表名/字段，输出：
- `app/models/<name>.py`（ORM 模型草案）
- `app/schemas/<name>.py`（Pydantic Create/Update/Response 草案）
- `app/services/<name>.py`（Service 草案）
- `app/routes/v1/<plural>.py`（Router 草案）
- `app/registry/builtin.py` 的 EntitySpec 追加片段
- `app/routes/v1/__init__.py` 的 include_router 追加片段
- Alembic revision 草案
- 基础测试草案

**实现策略**：用 `jinja2` 模板（不入主依赖，脚本独立运行）或纯字符串拼接。

**MVP 范围**：只支持 DB-only + hard_tombstone 实体。Note/Folder/Relation 类特殊实体拒绝生成，提示 `--custom-service` 标记。

**测试**：`tests/test_scaffold_entity.py` 验证生成文件结构（不实际写入磁盘，用 `dry_run=True`）。

### P3.2 check_entity_consistency.py

**新建** `backend/scripts/check_entity_consistency.py`：

CI 可调用的检查脚本，运行所有 parity test 并输出结构化报告。退出码：
- 0: 全部一致
- 1: 有不一致

**实现**：直接调用 pytest：
```python
import subprocess
result = subprocess.run(
    ["uv", "run", "pytest", "tests/test_parity_*.py", "-v", "--capture=no", "-p", "no:cacheprovider"],
    cwd=backend_dir,
)
sys.exit(result.returncode)
```

### P3.3 Goal dry-run spike

**目的**：用 `goal` 实体验证脚手架流程可用。

**步骤**：
1. 运行 `python scripts/scaffold_entity.py --name goal --class-name Goal --table-name goals --route-prefix goals --fields "title:string,due_date:datetime,completed:boolean"`
2. 检查生成的文件
3. 把生成的代码合并到主代码库（手工 review）
4. 跑全量测试
5. **不合并到 main 分支**——这是 spike，验证完即丢弃

**注**：本步骤是验证性 spike，目的是发现脚手架的不足。如果脚手架已足够好，可以把 goal 作为真实新实体留下；否则丢弃。

### Phase 3 验收

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
uv run pytest tests/test_scaffold_entity.py tests/test_parity_*.py -v --capture=no -p no:cacheprovider
uv run python scripts/check_entity_consistency.py
```

## Phase 4: Stats/MCP 注册化

**目标**：消除 stats 维度漂移。

### P4.1 StatSpec 设计 + 测试

**RED** — 新建 `tests/test_stat_spec.py`：

```python
"""StatSpec 登记 + parity 增强。"""
from __future__ import annotations

from app.services.stats_spec import STAT_SPECS


def test_stat_specs_covers_all_rest_endpoints():
    """STAT_SPECS 必须覆盖所有 REST /stats 端点。"""
    rest_paths = {s.route_path for s in STAT_SPECS}
    expected = {
        "/overview", "/focus-trend", "/task-distribution", "/daily-detail",
        "/habit-summary", "/schedule-summary", "/note-summary",
    }
    missing = expected - rest_paths
    assert not missing, f"STAT_SPECS missing: {missing}"


def test_stat_specs_covers_all_mcp_tools():
    """STAT_SPECS 必须覆盖所有 MCP stats 工具（或显式 mcp_enabled=False）。"""
    mcp_names = {s.mcp_tool for s in STAT_SPECS if s.mcp_enabled}
    expected = {
        "get_stats_overview", "get_focus_trend", "get_task_distribution",
        "get_daily_detail", "get_habit_summary", "get_schedule_summary",
        "get_note_summary",
    }
    missing = expected - mcp_names
    assert not missing, f"STAT_SPECS MCP missing: {missing}"


def test_stat_specs_route_and_mcp_names_consistent():
    """每个 spec 的 route_path 和 mcp_tool 必须配对。"""
    for spec in STAT_SPECS:
        if spec.mcp_enabled:
            assert spec.mcp_tool is not None, (
                f"{spec.name}: mcp_enabled=True but mcp_tool=None"
            )
```

**Verify RED**：测试 FAIL，因为 `app.services.stats_spec` 模块不存在。

**GREEN** — 新建 `app/services/stats_spec.py`：

```python
"""StatSpec — 统计维度的统一登记。

替代 StatsService 方法名、REST 路由、MCP 工具名三处手工同步。
"""
from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class StatSpec:
    """单个统计维度的元数据。"""
    name: str                # 维度名，如 "daily_detail"
    service_method: str      # StatsService 方法名，如 "daily_detail"
    route_path: str          # REST 路由，如 "/daily-detail"
    mcp_tool: str            # MCP 工具名，如 "get_daily_detail"
    mcp_enabled: bool = True
    params: dict = None      # 参数约束，如 {"days": {"default": 30, "ge": 1, "le": 365}}


STAT_SPECS: tuple[StatSpec, ...] = (
    StatSpec(
        name="overview",
        service_method="overview",
        route_path="/overview",
        mcp_tool="get_stats_overview",
        params={"periods": {"default": None}},
    ),
    StatSpec(
        name="focus_trend",
        service_method="focus_trend",
        route_path="/focus-trend",
        mcp_tool="get_focus_trend",
        params={"days": {"default": 7, "ge": 1, "le": 365}},
    ),
    StatSpec(
        name="task_distribution",
        service_method="task_distribution",
        route_path="/task-distribution",
        mcp_tool="get_task_distribution",
    ),
    StatSpec(
        name="daily_detail",
        service_method="daily_detail",
        route_path="/daily-detail",
        mcp_tool="get_daily_detail",
        params={"date": {"required": True, "max_length": 10}},
    ),
    StatSpec(
        name="habit_summary",
        service_method="habit_summary",
        route_path="/habit-summary",
        mcp_tool="get_habit_summary",
        params={"days": {"default": 30, "ge": 1, "le": 365}},
    ),
    StatSpec(
        name="schedule_summary",
        service_method="schedule_summary",
        route_path="/schedule-summary",
        mcp_tool="get_schedule_summary",
        params={"days": {"default": 30, "ge": 1, "le": 365}},
    ),
    StatSpec(
        name="note_summary",
        service_method="note_summary",
        route_path="/note-summary",
        mcp_tool="get_note_summary",
    ),
)
```

**Verify GREEN**：测试 PASS。

### P4.2 增强 parity test 用 STAT_SPECS 驱动

**修改** `tests/test_parity_stats_mcp.py`：用 `STAT_SPECS` 替代硬编码 `EXPECTED_MAPPING`：

```python
from app.services.stats_spec import STAT_SPECS


def test_mcp_has_all_stats_tools():
    """MCP server 必须注册所有 STAT_SPECS 中 mcp_enabled=True 的工具。"""
    import inspect
    import app.mcp.server as mcp_module
    expected = {spec.mcp_tool for spec in STAT_SPECS if spec.mcp_enabled}
    actual = {
        name for name, obj in inspect.getmembers(mcp_module)
        if callable(obj) and getattr(obj, "_mcp_tool", False)
    }
    missing = expected - actual
    assert not missing, f"MCP missing stats tools: {missing}"
```

**Verify**：测试 PASS。

### Phase 4 验收

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
uv run pytest tests/test_stat_spec.py tests/test_parity_stats_mcp.py -v --capture=no -p no:cacheprovider
```

## 全局验收

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
uv run pytest --capture=no -p no:cacheprovider -q
```

**通过标准**：
1. 所有 361 + 新增测试全绿（预计 ~390 测试）
2. `SyncService.ENTITY_REGISTRY` 内部由 `build_sync_registry()` 派生
3. `trash._ENTITY_MAP` 由 `REGISTRY.list_soft_delete()` 派生
4. `EntitySpec` 含 7 个新字段 + 2 个 effective_* 属性
5. 6 个 parity test 文件 + 2 个新测试文件（entity_spec_extension、build_sync_registry、stat_spec）
6. MCP `get_daily_detail` 工具存在且可执行
7. `scaffold_entity.py` + `check_entity_consistency.py` 可运行
8. `核心文档/15-扩展性parity例外表.md` 存在

## 完成后行动

1. 更新 `c:\Users\20564\.trae-cn\memory\projects\-e-Development-MyAwesomeApp-PomodoroXII\project_memory.md`：
   - Phase F-1（扩展性 4.5 星）进度 = 100%
   - 测试总数更新
   - 关键文件位置追加 `app/registry/resolve.py`、`app/registry/sync_registry.py`、`app/services/stats_spec.py`、`scripts/scaffold_entity.py`、`scripts/check_entity_consistency.py`
2. Git commit：`feat: extensibility 4.5-star improvement (parity gates + registry-derived sync/trash + StatSpec)`
3. 不 push（除非用户要求）

## 文件清单

### 新建文件（11 个）

| 路径 | 用途 |
|---|---|
| `backend/app/registry/resolve.py` | `resolve_model()` + `import_all_models()` |
| `backend/app/registry/sync_registry.py` | `build_sync_registry()` |
| `backend/app/services/stats_spec.py` | `StatSpec` + `STAT_SPECS` |
| `backend/scripts/scaffold_entity.py` | 实体脚手架 CLI |
| `backend/scripts/check_entity_consistency.py` | 一致性检查脚本 |
| `backend/tests/test_parity_registry_sync.py` | Registry vs Sync parity |
| `backend/tests/test_parity_registry_orm.py` | Registry vs ORM parity |
| `backend/tests/test_parity_registry_schemas.py` | Registry vs Schema parity |
| `backend/tests/test_parity_routes.py` | Registry vs Routes parity |
| `backend/tests/test_parity_stats_mcp.py` | Stats REST vs MCP parity |
| `backend/tests/test_parity_alembic_metadata.py` | Alembic vs metadata parity |
| `backend/tests/test_entity_spec_extension.py` | EntitySpec 扩展字段测试 |
| `backend/tests/test_build_sync_registry.py` | build_sync_registry 测试 |
| `backend/tests/test_stat_spec.py` | StatSpec 测试 |
| `backend/tests/test_scaffold_entity.py` | 脚手架测试 |
| `核心文档/15-扩展性parity例外表.md` | 例外表文档 |

### 修改文件（5 个）

| 路径 | 改动 |
|---|---|
| `backend/app/registry/entities.py` | EntitySpec 加 7 字段 + 2 effective_* 属性 |
| `backend/app/registry/builtin.py` | 14 实体追加 sync_entity_type/pull_key/delete_strategy |
| `backend/app/services/sync.py` | ENTITY_REGISTRY 改为 build_sync_registry() 派生 |
| `backend/app/routes/v1/trash.py` | _ENTITY_MAP 改为 REGISTRY.list_soft_delete() 派生 |
| `backend/app/mcp/server.py` | 追加 get_daily_detail MCP 工具 |
