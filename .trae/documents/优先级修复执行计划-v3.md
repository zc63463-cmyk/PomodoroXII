# PomodoroXII 优先级修复执行计划 v3

> **生成时间**: 2026-07-04（Plan Mode Phase 3）
> **方法论**: TDD (Red → Green → Refactor)
> **Skill 协同**: `test-driven-development` (TDD 流程引导) + `agent-browser` (C10 可选端到端)
> **基线测试数**: 292（P0+P1.1+P1.2 完成后实际全量）
> **计划目标**: 完成 P1.3 + Phase C (C1-C10) + P3 剩余项，预期新增 ~73 测试

---

## 一、当前状态分析

### 1.1 进度基线（基于本次探索）

| 任务 | 状态 | 证据 |
|------|------|------|
| P0 创建 project_memory.md | ✅ 完成 | `c:\Users\20564\.trae-cn\memory\projects\-e-Development-MyAwesomeApp-PomodoroXII\project_memory.md` 已存在 |
| P1.1 P2-1 修复 11 个 list 端点 | ✅ 完成 | 10 个路由文件已改为 `PaginatedResponse[T]`，`tests/test_routes_pagination.py` 含 11 测试 |
| P1.2 P2-5 sync 索引 | ✅ 完成 | `sync_outbox.py`/`sync_audit_log.py` 字段含 `index=True`；`alembic/versions/002_sync_indexes.py` 已创建；`tests/test_sync_indexes.py` 含 2 测试 |
| P1.3 P2-6 Mixin onupdate + version | 🔄 进行中 | `mixins.py` L28 仍为 `default=utc_now_iso`（无 `onupdate`）；`base.py` L69-77 未自增 version |
| C1-C10 Phase C 实施 | ❌ 未开始 | `app/services/sync.py` 不存在；`app/services/sync_safety.py` 不存在；`app/routes/v1/sync.py` 不存在；`app/schemas/sync.py` 不存在 |
| P3.1-P3.6 剩余项 | ❌ 未开始 | task.py/serializers.py/deps.py/note.py 均未修改 |

### 1.2 关键架构约束（铁律）

1. **三层铁律**: Routers commit / Services flush / Models 纯数据 — Service 不导入 fastapi，不调 commit
2. **双 Base 隔离**: `app.db.base.Base` (业务) vs `app.file_system.schema.Base` (FS 索引)
3. **双 JWT 认证**: Master Token (7d) + Space Token (8h, 含 space_id)
4. **Note 模型无 content 字段**: .md 文件是唯一 Source of Truth，DB 仅存 `content_hash` + `word_count`
5. **Saga Try-Compensate**: NoteService create/update_content/delete 三方法均含补偿
6. **Tombstone TOCTOU 防护**: `TombstoneService.create` 用 `try/except IntegrityError` 处理竞态

### 1.3 关键文件位置

| 文件 | 用途 |
|------|------|
| [mixins.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/models/mixins.py) | SyncMixin (id/created_at/updated_at/version) — P1.3 待修改 |
| [base.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/base.py) | BaseService.update — P1.3 待修改 |
| [note.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/note.py) | NoteService — C6 待修改 |
| [task.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/task.py) | TaskService — P3.1 待修改 |
| [tombstone.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/tombstone.py) | TombstoneService — C9 复用 |
| [deps.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/deps.py) | get_space_context — P3.6 待修改 |
| [serializers.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/serializers.py) | serialize_entity — P3.3 待修改 |
| [routes/v1/__init__.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/__init__.py) | 15 路由挂载 — C7 待追加 sync_router |
| [conftest.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/tests/conftest.py) | `space_session` + `client` fixture + `_isolate_env` autouse |
| 详细计划文档 | [优先级修复与PhaseC实施计划.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/.trae/documents/优先级修复与PhaseC实施计划.md) (809 行，含 C1-C10 完整代码模板) |

---

## 二、P1.3 — Mixin onupdate + version 自增（2 测试）

### 2.1 修改 1: mixins.py

**文件**: `e:\Development\MyAwesomeApp\PomodoroXII\backend\app\models\mixins.py`

**当前 L28**:
```python
updated_at: Mapped[str] = mapped_column(String(32), default=utc_now_iso)
```

**修改为**:
```python
updated_at: Mapped[str] = mapped_column(String(32), default=utc_now_iso, onupdate=utc_now_iso)
```

**理由**: 添加 SQL 层 `onupdate` 作为安全网，即使 Service 层忘记设置 `updated_at`，DB 也会自动刷新。已在 `BaseService.update` L74 手动设置，因此冗余但无害。

### 2.2 修改 2: base.py BaseService.update

**文件**: `e:\Development\MyAwesomeApp\PomodoroXII\backend\app\services\base.py`

**当前 L69-77**:
```python
async def update(self, id: str, data: dict[str, Any]) -> Any:
    obj = await self.get(id)
    for k, v in data.items():
        setattr(obj, k, v)
    obj.updated_at = utc_now_iso()
    await self.db.flush()
    await self.db.refresh(obj)
    return obj
```

**修改为**:
```python
async def update(self, id: str, data: dict[str, Any]) -> Any:
    obj = await self.get(id)
    for k, v in data.items():
        setattr(obj, k, v)
    obj.updated_at = utc_now_iso()
    if hasattr(obj, "version"):
        obj.version = (obj.version or 0) + 1
    await self.db.flush()
    await self.db.refresh(obj)
    return obj
```

**理由**: 实现乐观并发控制 (Optimistic Concurrency Control) 的 version 自增。`hasattr` 防护避免影响非 SyncMixin 模型（如 SyncOutbox/SyncAuditLog/Tombstone）。

### 2.3 TDD 流程

**Red → Green 顺序**:
1. 先写测试追加到 `tests/test_base_service.py` 末尾（Red）
2. 修改 `mixins.py` + `base.py`（Green）
3. 运行 `pytest tests/test_base_service.py -k "version or updated_at" -v`

**测试 1: test_update_bumps_version**
```python
@pytest.mark.asyncio
async def test_update_bumps_version(space_session):
    """BaseService.update 应自增 version 字段。"""
    from app.services.base import BaseService
    from app.models.task import Task

    class TaskService(BaseService):
        model = Task

    svc = TaskService(space_session)
    obj = await svc.create({
        "id": uuid.uuid4().hex,
        "title": "V1",
        "status": "todo",
        "priority": "medium",
        "tags": "[]",
    })
    assert obj.version == 1
    updated = await svc.update(obj.id, {"title": "V2"})
    assert updated.version == 2
    assert updated.title == "V2"
```

**测试 2: test_update_refreshes_updated_at**
```python
@pytest.mark.asyncio
async def test_update_refreshes_updated_at(space_session):
    """BaseService.update 应刷新 updated_at（允许 onupdate 或显式设置）。"""
    from app.services.base import BaseService
    from app.models.task import Task

    class TaskService(BaseService):
        model = Task

    svc = TaskService(space_session)
    obj = await svc.create({
        "id": uuid.uuid4().hex,
        "title": "T1",
        "status": "todo",
        "priority": "medium",
        "tags": "[]",
    })
    original_ts = obj.updated_at
    updated = await svc.update(obj.id, {"title": "T2"})
    assert updated.updated_at is not None
    assert updated.updated_at.endswith("Z")
    # 同秒内可能相等，仅验证非空且格式正确
```

### 2.4 验证

```powershell
cd 'e:\Development\MyAwesomeApp\PomodoroXII\backend'
.venv\Scripts\python.exe -m pytest tests/test_base_service.py -k "version or updated_at" -v
.venv\Scripts\python.exe -m pytest -q  # 全量回归预期 294 全绿
```

---

## 三、P2 阶段 — Phase C 实施（C1-C10，~54 新测试）

### 3.1 任务依赖图

```
C1 sync_safety.py ─────── 纯工具函数,无依赖（12 测试）
    │
    ▼
C2 SyncService.push ──── 依赖 C1（5 测试）
    │
    ▼
C3 SyncService.pull ──── 依赖 C2（5 测试）
    │
    ├──▶ C5 SAVEPOINT 兼容性 ── 纯测试（3 测试）
    ├──▶ C8 ENTITY_REGISTRY 验证 ── 纯测试（3 测试）
    │
    ▼
C4 SyncService.full + status ── 依赖 C3（4 测试）
    │
    ├──▶ C6 NoteService sync_mode ── 依赖 C3（4 测试）
    │        │
    │        ▼
    └──▶ C9 sync 审计 ── 依赖所有方法（3 测试）
              │
              ▼
        C7 sync 路由 ── 依赖全部（7 测试）
              │
              ▼
        C10 集成测试 ── 依赖路由（8 测试, agent-browser 可选）
```

### 3.2 C1: sync_safety.py — 纯工具函数（12 测试）

**新建文件**: `backend/app/services/sync_safety.py`

**函数清单**:
- `normalize_timestamp(ts: str) -> str` — 归一化到毫秒精度 ISO 字符串
- `is_zero_time(ts: str) -> bool` — 检测零时间戳（`""`、`"0"`、`None`、`"1970-01-01T00:00:00Z"`）
- `sanitize_zero_time(ts: str, now: str | None = None) -> str` — 零时间替换为当前 UTC
- `serialize_entity(obj: Any) -> dict` — ORM → dict（处理 tags JSON 字符串 → list）
- `check_lww_conflict(local_obj: Any, remote_ts: str) -> str` — LWW 冲突检测，返回 `"local"`/`"remote"`/`"equal"`

**约束**:
- 不导入 fastapi
- 不调 commit
- 依赖: `from app.services.time import utc_now_iso`
- 复用 `app.services.serializers.serialize_entity` 的 tags 解析逻辑

**TDD 流程**:
1. 写 `tests/test_sync_safety.py`（12 测试，覆盖 5 函数各正/负/边界）
2. 实现 `sync_safety.py`
3. 验证: `pytest tests/test_sync_safety.py -v`

### 3.3 C2: SyncService.push + ENTITY_REGISTRY + schemas（5 测试）

**新建文件**:
- `backend/app/schemas/sync.py` — SyncEvent / SyncPushRequest / SyncPushResponse / SyncPullResponse / SyncFullResponse / SyncStatusResponse
- `backend/app/services/sync.py` — SyncService 类 + ENTITY_REGISTRY

**ENTITY_REGISTRY 内容**（14 实体）:
```python
ENTITY_REGISTRY = {
    "task": {"model": Task, "pull_key": "tasks"},
    "session": {"model": Session, "pull_key": "sessions"},
    "note": {"model": Note, "pull_key": "notes"},
    "folder": {"model": Folder, "pull_key": "folders"},
    "quickNote": {"model": QuickNote, "pull_key": "quickNotes"},
    "reflection": {"model": Reflection, "pull_key": "reflections"},
    "habit": {"model": Habit, "pull_key": "habits"},
    "habitCheckIn": {"model": HabitCheckIn, "pull_key": "habitCheckIns"},
    "schedule": {"model": Schedule, "pull_key": "schedules"},
    "timeBlock": {"model": TimeBlock, "pull_key": "timeBlocks"},
    "memoComment": {"model": MemoComment, "pull_key": "memoComments"},
    "sessionQuickNote": {"model": SessionQuickNote, "pull_key": "sessionQuickNotes"},
    "scheduleQuickNote": {"model": ScheduleQuickNote, "pull_key": "scheduleQuickNotes"},
    "taskQuickNote": {"model": TaskQuickNote, "pull_key": "taskQuickNotes"},
}
```

**SyncService.push 签名**: `async def push(self, events: list[dict]) -> dict[str, Any]`

**返回结构**: `{"applied": [...], "conflicts": [...], "errors": [...], "server_time": "..."}`

**实现要点**:
- 逐事件 `async with self.db.begin_nested()` (SAVEPOINT 隔离)
- 调用 sync_safety 安全检查（normalize_timestamp / sanitize_zero_time / check_lww_conflict）
- Note 实体走 NoteService（C6 实现后；C2 阶段先用占位 stub）
- 其他实体走直接 ORM 操作（get/insert/update/delete）
- 不 commit（路由 commit）

**TDD 流程**:
1. 写 `tests/test_sync_service.py`（5 个 push 测试：单事件 create/update/delete、批量事件、LWW 冲突）
2. 实现 `sync.py` 的 push() 方法 + `schemas/sync.py`
3. 验证: `pytest tests/test_sync_service.py -k push -v`

### 3.4 C3: SyncService.pull（5 测试）

**修改文件**: `backend/app/services/sync.py`（追加 pull 方法）

**pull 签名**: `async def pull(self, since: str = "", limit: int = 1000) -> dict`

**返回结构**: `{server_time, has_more, next_since, tombstones, tasks, sessions, ...}`（14 个 pull_key 分组）

**实现要点**:
- 遍历 ENTITY_REGISTRY，每实体查询 `updated_at > since`
- Note 特殊处理: 收集 note_ids → `fs.read_notes_batch(note_ids)` 批量读 content（如有 fs；否则仅返回元数据）
- Tombstone 查询 `deleted_at > since`
- 分页: `limit + 1` 条用于检测 has_more

**TDD 流程**:
1. 写 5 个 pull 测试（空 since 拉全量、since 过滤、has_more 分页、tombstones 返回、note content 拼接）
2. 实现 pull() 方法
3. 验证: `pytest tests/test_sync_service.py -k pull -v`

### 3.5 C4: SyncService.full + status（4 测试）

**修改文件**: `backend/app/services/sync.py`（追加 full + status 方法）

**full()**: 与 pull 的区别是 tombstones 不按 since 过滤（全量返回），附加 `"is_full": True`

**status()**: 返回 `{server_time, entity_counts: {tasks: N, sessions: N, ...}, tombstone_count: N}`

**TDD 流程**:
1. 写 4 个 full/status 测试
2. 实现 full() + status()
3. 验证: `pytest tests/test_sync_service.py -k "full or status" -v`

### 3.6 C5: SAVEPOINT 兼容性验证（3 测试，纯测试）

**修改文件**: `backend/tests/test_note_service.py`（追加 3 个测试，无新代码）

**测试目的**: 验证 NoteService Saga (create/update_content/delete) 在 `db.begin_nested()` SAVEPOINT 内正确工作，compensate 不破坏外层事务

**TDD 流程**:
1. 写 3 个 savepoint 测试（create + rollback、update_content + rollback、delete + rollback）
2. 验证: `pytest tests/test_note_service.py -k savepoint -v`

### 3.7 C6: NoteService sync_mode 集成（4 测试）

**修改文件**:
- `backend/app/services/note.py`（添加 sync_mode 参数）
- `backend/app/services/sync.py`（push 中 note 事件委托给 NoteService）

**NoteService 改动**:
- `__init__` 添加 `sync_mode: bool = False` 参数
- sync_mode=True 时保留客户端 `updated_at`/`version`/`created_at`（不覆盖）
- sync_mode=True 时不写新 tombstone（保留远端 tombstone 决策）

**SyncService 改动**:
- 新增 `_push_note_event(event)` 辅助方法
- push() 中 `etype == "note"` 委托给 `_push_note_event()`
- `_push_note_event` 内部实例化 `NoteService(db, fs, sync_mode=True)`

**TDD 流程**:
1. 写 4 个 sync_mode 测试（保留 client updated_at、保留 client version、不写 tombstone、create/update/delete 各一）
2. 实现 sync_mode 参数 + _push_note_event
3. 验证: `pytest tests/test_sync_service.py -k sync_mode -v`

### 3.8 C7: sync 路由（7 测试）

**新建文件**: `backend/app/routes/v1/sync.py`

**修改文件**: `backend/app/routes/v1/__init__.py`（注册 sync_router）

**端点**:
- `POST /api/v1/sync/push` — body: SyncPushRequest, 返回 SyncPushResponse
- `GET /api/v1/sync/pull?since=&limit=` — 返回 SyncPullResponse
- `GET /api/v1/sync/full?since=&limit=` — 返回 SyncFullResponse
- `GET /api/v1/sync/status` — 返回 SyncStatusResponse

**约束**:
- 路由 commit（Service 仅 flush）
- 使用 `Depends(get_space_db)` + `Depends(get_space_context)` + `Depends(get_file_system)`
- 调用 `SyncService(db, fs).push/pull/full/status`

**TDD 流程**:
1. 写 `tests/test_sync_routes.py`（7 个测试：4 端点各 happy path + push 空数组 + pull 分页 + status 计数）
2. 实现 sync 路由
3. 注册到 `__init__.py`（追加 `from app.routes.v1.sync import router as sync_router` + `router.include_router(sync_router, prefix="/sync", tags=["sync"])`）
4. 验证: `pytest tests/test_sync_routes.py -v`

### 3.9 C8: ENTITY_REGISTRY 验证（3 测试，纯测试）

**修改文件**: `backend/tests/test_sync_service.py`（追加 3 个测试，无新代码）

**测试目的**:
1. 验证 registry 含 14 个实体（key 集合完整）
2. 验证每个 registry 项有 `model` 和 `pull_key` 字段
3. 验证 pull_key 与 sync pull 响应字段一致

**TDD 流程**:
1. 写 3 个 entity_registry 测试
2. 验证: `pytest tests/test_sync_service.py -k entity_registry -v`

### 3.10 C9: sync 审计（3 测试）

**修改文件**: `backend/app/services/sync.py`（添加 `_write_audit` 方法）

**_write_audit 实现**:
- 在 savepoint 外执行（审计失败不影响已应用事件）
- 写入 SyncAuditLog: event_type / entity_type / entity_id / details
- try/except 包裹（失败仅记录日志，不抛出）

**调用点**:
- push() 中每事件后调用 `_write_audit("push", etype, eid, ...)`
- pull() 末尾调用 `_write_audit("pull", "batch", "", ...)`
- status() 不写审计（高频只读）

**TDD 流程**:
1. 写 3 个审计测试（push 后有审计、pull 后有审计、审计失败不影响主流程）
2. 实现 _write_audit
3. 在 push/pull 中调用
4. 验证: `pytest tests/test_sync_service.py -k audit -v`

### 3.11 C10: 集成测试（8 测试，agent-browser 可选）

**新建文件**: `backend/tests/test_sync_integration.py`

**测试场景**（8 个）:
1. 端到端 push + pull 双向同步
2. LWW 冲突解决（client ts > server ts 时客户端覆盖）
3. Tombstone 防复活（pull 返回 tombstone，client 不再 push 旧版本）
4. Note content 双存储一致性（push note → DB + .md 都更新）
5. SAVEPOINT 隔离下批量 push（一事件失败不阻断其他）
6. pull 分页 has_more（limit=2 + 5 条数据 → has_more=True）
7. status 计数正确性（创建 3 task + 2 note → status 返回正确计数）
8. 路由端到端（使用 `client` fixture 模拟真实 HTTP 调用）

**Skill 使用**: `agent-browser` 可选用于场景 8 的浏览器端到端验证（如果需要前端联调）；当前前端不存在，默认使用 `client` fixture 即可

**TDD 流程**:
1. 写 8 个集成测试
2. 验证: `pytest tests/test_sync_integration.py -v`

### 3.12 Phase C 完成验收

**全量回归**: `pytest -q`（预期 294 + ~54 = ~348 测试全绿）

**三层铁律检查**:
```powershell
cd 'e:\Development\MyAwesomeApp\PomodoroXII\backend'
.venv\Scripts\python.exe -c "import pathlib; [print(f) for f in pathlib.Path('app/services').glob('*.py') if 'fastapi' in f.read_text(encoding='utf-8')]"
.venv\Scripts\python.exe -c "import pathlib; [print(f) for f in pathlib.Path('app/services').glob('*.py') if '.commit()' in f.read_text(encoding='utf-8')]"
# 两个命令都应返回空
```

---

## 四、P3 阶段 — 修复剩余 P2/P3 项（6 任务 6 测试）

### 4.1 P3.1 — P2-2 TaskService.update 处理 tags（1 测试）

**文件**: `backend/app/services/task.py`

**当前问题**: TaskService.update 继承自 BaseService.update，不处理 tags list → JSON 转换。如果客户端传 `tags: ["work"]` 会写入 list 类型，与 DB schema (String) 不符。

**修复**: 重写 TaskService.update，参考 NoteService.update_metadata 的 tags 处理:
```python
async def update(self, id: str, data: dict[str, Any]) -> Any:
    data = dict(data)
    if "tags" in data and isinstance(data["tags"], list):
        data["tags"] = json.dumps(data["tags"])
    return await super().update(id, data)
```

**TDD 流程**:
1. 写测试 `test_update_task_converts_tags_list_to_json`
2. 实现
3. 验证: `pytest tests/test_task_service.py -k update_tags -v`

### 4.2 P3.2 — P2-3 trash.py purge_item N+1 修复（1 测试）

**文件**: `backend/app/routes/v1/trash.py`

**当前问题**: `purge_item` 可能对每个 descendant 单独查询/删除，导致 N+1。

**修复**: 改为 `select(Folder).where(Folder.id.in_(desc_ids))` 批量查询 + `delete(Folder).where(Folder.id.in_(desc_ids))` 批量删除。

**TDD 流程**:
1. 写测试 `test_purge_item_batch_query_no_n_plus_1`（用 `pytest.mark.asyncio` + 计数 query 次数或验证一次 query 完成）
2. 实现
3. 验证: `pytest tests/test_routes_v1.py -k purge -v`

### 4.3 P3.3 — P2-4 serializers json.loads 保护（1 测试）

**文件**: `backend/app/services/serializers.py`

**当前问题**: `serialize_entity` L21 `json.loads(d["tags"])` 未保护，若 tags 字段是损坏 JSON 会抛 `JSONDecodeError`。

**修复**: 添加 try/except:
```python
if "tags" in d and isinstance(d["tags"], str):
    try:
        d["tags"] = json.loads(d["tags"]) if d["tags"] else []
    except (json.JSONDecodeError, ValueError):
        d["tags"] = []
```

**TDD 流程**:
1. 写测试 `test_serialize_handles_malformed_tags_json`
2. 实现
3. 验证: `pytest tests/test_serializers.py -v`

### 4.4 P3.4 — P3-10 Note status CheckConstraint（1 测试）

**文件**: `backend/app/models/note.py`

**当前问题**: Note.status 无 DB 层 CheckConstraint，可写入任意字符串。

**修复**: 添加 `CheckConstraint("status IN ('active','archived')", name="check_note_status")` 到 `__table_args__`。

**TDD 流程**:
1. 写测试 `test_note_invalid_status_raises`（尝试创建 status='invalid' 的 note → 应抛 IntegrityError）
2. 实现
3. 验证: `pytest tests/test_models.py -k note_status -v`

### 4.5 P3.5 — P3-11 Task 字段索引（1 测试）

**文件**: `backend/app/models/task.py`

**修复**: 为 `status`、`priority`、`due_date` 字段添加 `index=True`。

**TDD 流程**:
1. 写测试 `test_task_has_indexes_on_query_fields`
2. 实现
3. 验证: `pytest tests/test_models.py -k task_index -v`

### 4.6 P3.6 — P3-13 deps.py space_id 校验（1 测试）

**文件**: `backend/app/deps.py`

**当前问题**: `get_space_context` L66-69 仅校验 token 含 space_id，不校验 space_id 对应的 space 是否真实存在。

**修复**: 在返回前调用 `space_manager.get_space(space_id)`，不存在则抛 `NotFoundError`:
```python
async def get_space_context(user: dict = Depends(get_current_user)) -> dict[str, Any]:
    if user.get("type") != "space":
        raise AuthorizationError("Space token required")
    space_id = user.get("space_id")
    if not space_id:
        raise AuthenticationError("Space token missing space_id")
    # 校验 space 存在
    from app.space_manager import get_space_engine_manager
    manager = get_space_engine_manager()
    if space_id not in manager.spaces:  # 或调用 space 查询方法
        raise NotFoundError(f"Space '{space_id}' not found")
    return {"space_id": str(space_id), "user_id": str(user.get("sub"))}
```

**TDD 流程**:
1. 写测试 `test_get_space_context_raises_on_unknown_space_id`
2. 实现
3. 验证: `pytest tests/test_deps.py -v`

---

## 五、执行计划（任务清单）

| 阶段 | 任务 | 工具 | 优先级 | 估算测试 |
|------|------|------|--------|---------|
| P1.3 | P2-6 Mixin onupdate + version | Edit mixins.py + base.py + TDD | P1 | 2 测试 |
| P2.1 (C1) | sync_safety.py | Write + TDD | P2 | 12 测试 |
| P2.2 (C2) | SyncService.push + schemas | Write sync.py + schemas/sync.py + TDD | P2 | 5 测试 |
| P2.3 (C3) | SyncService.pull | Edit sync.py + TDD | P2 | 5 测试 |
| P2.4 (C4) | SyncService.full + status | Edit sync.py + TDD | P2 | 4 测试 |
| P2.5 (C5) | SAVEPOINT 兼容性测试 | TDD only (test_note_service.py) | P2 | 3 测试 |
| P2.6 (C6) | NoteService sync_mode | Edit note.py + sync.py + TDD | P2 | 4 测试 |
| P2.7 (C7) | sync 路由 | Write routes/v1/sync.py + Edit __init__.py + TDD | P2 | 7 测试 |
| P2.8 (C8) | ENTITY_REGISTRY 验证 | TDD only (test_sync_service.py) | P2 | 3 测试 |
| P2.9 (C9) | sync 审计 | Edit sync.py + TDD | P2 | 3 测试 |
| P2.10 (C10) | 集成测试 | Write test_sync_integration.py + TDD + agent-browser 可选 | P2 | 8 测试 |
| P3.1 | P2-2 TaskService update tags | Edit task.py + TDD | P3 | 1 测试 |
| P3.2 | P2-3 trash.py purge N+1 | Edit trash.py + TDD | P3 | 1 测试 |
| P3.3 | P2-4 serializers json protection | Edit serializers.py + TDD | P3 | 1 测试 |
| P3.4 | P3-10 Note status CheckConstraint | Edit note.py + TDD | P3 | 1 测试 |
| P3.5 | P3-11 Task 字段索引 | Edit task.py + TDD | P3 | 1 测试 |
| P3.6 | P3-13 deps space_id 校验 | Edit deps.py + TDD | P3 | 1 测试 |
| **合计** | 17 任务 | | | **~63 新测试** |

---

## 六、假设与决策

### 6.1 假设

1. `PaginatedResponse` schema 已存在且已使用（P1.1 完成）
2. sync infra 索引已添加（P1.2 完成）
3. `space_session` / `client` fixture 已就绪（已在 conftest.py 确认）
4. Phase C 详细代码模板已在 `优先级修复与PhaseC实施计划.md` (809 行) 中提供
5. test-driven-development Skill 提供 TDD Red-Green-Refactor 流程引导
6. agent-browser Skill 在 C10 可选用于端到端验证（前端未存在时使用 client fixture）
7. 全程遵守三层铁律（Service 不导入 fastapi，不调 commit）

### 6.2 决策

- **D1**: P1.3 使用 `hasattr(obj, "version")` 防护，避免影响无 SyncMixin 的模型
- **D2**: P1.3 添加 `onupdate=utc_now_iso` 作为冗余安全网（已在 BaseService.update 显式设置）
- **D3**: Phase C 10 任务严格按依赖顺序执行（C1→C2→C3→C4→C5+C8 并行→C6→C9→C7→C10）
- **D4**: 每个任务先写测试（Red），再实现（Green），最后重构（Refactor）
- **D5**: C2 阶段 NoteService sync_mode 未实现时，push 中 note 事件用占位 stub（直接 ORM 操作），C6 实现后替换为 NoteService 委托
- **D6**: C10 不强制使用 agent-browser（前端未存在，使用 client fixture 即可）
- **D7**: 全部任务完成后更新 project_memory.md 标注 Phase C 完成
- **D8**: TDD Skill 在每个 Phase C 任务开始时调用，引导 Red-Green-Refactor 流程
- **D9**: 不修改 alembic/env.py 的 include_object 过滤器（已正确实现 P0-1）
- **D10**: P3.4 CheckConstraint 需新建 Alembic migration `003_note_status_check.py`（DB 层约束）
- **D11**: P3.5 Task 字段索引需新建 Alembic migration `004_task_indexes.py`

### 6.3 不做的事

- ❌ 不修改 session_memory_*.jsonl 或 topics.md
- ❌ 不修改 user_profile.md
- ❌ 不创建除计划文件和必要代码文件之外的新文档
- ❌ 不主动启动 Phase D-H
- ❌ 不修改 cognee 索引
- ❌ 不重写主报告 `项目深度调研分析报告.md`
- ❌ 不重写 P0/P1.1/P1.2 已完成的工作

### 6.4 Skill 使用规划

| Skill | 使用时机 | 用途 |
|-------|---------|------|
| `test-driven-development` | P1.3/P2/P3 每个任务开始时 | 引导 TDD Red-Green-Refactor 流程 |
| `agent-browser` | C10 集成测试阶段（可选） | 浏览器端到端验证 sync 路由（前端未存在时跳过） |

---

## 七、验证步骤

### 7.1 P1.3 验证

```powershell
cd 'e:\Development\MyAwesomeApp\PomodoroXII\backend'
.venv\Scripts\python.exe -m pytest tests/test_base_service.py -k "version or updated_at" -v
.venv\Scripts\python.exe -m pytest -q  # 预期 294 全绿
```

### 7.2 P2 验证（Phase C）

```powershell
# 各任务测试
.venv\Scripts\python.exe -m pytest tests/test_sync_safety.py -v       # C1
.venv\Scripts\python.exe -m pytest tests/test_sync_service.py -v      # C2-C6, C8-C9
.venv\Scripts\python.exe -m pytest tests/test_note_service.py -k savepoint -v  # C5
.venv\Scripts\python.exe -m pytest tests/test_sync_routes.py -v        # C7
.venv\Scripts\python.exe -m pytest tests/test_sync_integration.py -v   # C10

# 全量回归
.venv\Scripts\python.exe -m pytest -q  # 预期 294 + ~54 = ~348 全绿

# 三层铁律检查
.venv\Scripts\python.exe -c "import pathlib; [print(f) for f in pathlib.Path('app/services').glob('*.py') if 'fastapi' in f.read_text(encoding='utf-8')]"
.venv\Scripts\python.exe -c "import pathlib; [print(f) for f in pathlib.Path('app/services').glob('*.py') if '.commit()' in f.read_text(encoding='utf-8')]"
# 两个命令都应返回空
```

### 7.3 P3 验证

```powershell
.venv\Scripts\python.exe -m pytest tests/test_task_service.py -k update_tags -v
.venv\Scripts\python.exe -m pytest tests/test_routes_v1.py -k purge -v
.venv\Scripts\python.exe -m pytest tests/test_serializers.py -v
.venv\Scripts\python.exe -m pytest tests/test_models.py -k "note_status or task_index" -v
.venv\Scripts\python.exe -m pytest tests/test_deps.py -v

# 全量最终回归
.venv\Scripts\python.exe -m pytest -q  # 预期 ~354 全绿
```

### 7.4 收尾

1. 更新 `project_memory.md` 标注 Phase C 完成 + 测试总数
2. 在 `topics.md` 记录本次完成情况（自动生成）

---

## 八、关键约束（铁律）

1. **三层铁律**: Routers commit / Services flush / Models 纯数据 — 严格遵守
2. **TDD 流程**: 每个任务先写测试（Red），再实现（Green），最后重构（Refactor）
3. **MCP 工具规范**: 调用 run_mcp 时严格使用 `{"server_name": ..., "tool_name": ..., "args": {...}}` 格式
4. **代码引用规范**: 所有代码引用使用 `file:///` 链接格式
5. **不创建非必要文件**: 仅创建计划要求的代码文件 + 测试文件 + 必要 Alembic migration
6. **路径规范**: 当前仓库为 `e:\Development\MyAwesomeApp\PomodoroXII\backend`（大写 P 和 X）
7. **Skill 使用**: test-driven-development 用于 TDD 流程，agent-browser 用于端到端测试（可选）
8. **不动用户代码之外的范围**: 不修改 cognee 索引，不修改 session_memory，不修改 user_profile

---

## 九、风险与缓解

| 风险 | 严重度 | 缓解措施 |
|------|--------|---------|
| Phase C 详细代码模板与当前仓库不兼容 | 中 | 逐任务适配路径，先验证 import 路径（旧计划针对 pomodoroxi-rebuild 已归档） |
| TDD 流程耗时较长 | 中 | 严格遵守计划顺序，每任务完成后立即验证 |
| sync_mode 集成可能破坏现有 NoteService | 高 | 添加 sync_mode=False 默认值，向后兼容；现有测试全量回归 |
| Alembic migration 003/004 可能与现有冲突 | 低 | P1.2 已验证 002 成功；003/004 沿用相同模式 |
| agent-browser Skill 不可用或前端未存在 | 低 | C10 集成测试使用 client fixture 替代 |
| 测试总数增长后 conftest 隔离可能失效 | 中 | 每阶段全量回归验证（294 → 348 → 354） |
| NoteService Saga + sync_mode 在 SAVEPOINT 内行为复杂 | 高 | C5 专门验证 SAVEPOINT 兼容性 |
| ENTITY_REGISTRY 14 实体 import 路径错误 | 中 | C8 专门验证 registry 完整性 |
| sync 审计失败影响主流程 | 低 | C9 _write_audit 用 try/except 包裹，失败仅记录日志 |
