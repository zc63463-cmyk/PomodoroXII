# 扩展性 4.5 星提升 — 接续执行计划 v3

> **母计划**: [扩展性4.5星提升-实施计划-v2.md](./扩展性4.5星提升-实施计划-v2.md) · [扩展性4.5星提升-执行续作计划.md](./扩展性4.5星提升-执行续作计划.md)
> **执行模式**: TDD Red-Green-Refactor + 顺序 Phase 推进 + Phase 3/4 子任务并行
> **基线**: 361 tests passing + P1.2-P1.6 已完成（共新增 ~67 测试）
> **本计划定位**: 从 P1.7 起步到全局验收的接续执行清单

## 摘要

上一会话已完成 P1.2-P1.6（EntitySpec 加字段 + resolve.py + 4 个 parity test 文件 + get_daily_detail MCP 工具）。本计划承接，从 P1.7 起步，顺序推进 Phase 1 收尾 → Phase 2 (高风险，单线) → Phase 3/4 (低耦合，可派发并行 agent) → 全局验收。每个任务严格 TDD：先写一个失败测试，再写最小实现。

## 当前实际进度核实（已读代码确认）

| 任务 | 状态 | 证据 |
|---|---|---|
| P1.2 EntitySpec 加 sync_entity_type/pull_key | ✅ | [entities.py:73-74](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/entities.py#L73-L74) |
| P1.2 builtin 7 个 camelCase 实体补 sync_entity_type | ✅ | [builtin.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/builtin.py) 含 quickNote/habitCheckIn/timeBlock/memoComment/sessionQuickNote/scheduleQuickNote/taskQuickNote |
| P1.3 resolve.py + test_parity_registry_orm.py | ✅ | [resolve.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/resolve.py) 存在；test 文件 40 测试 PASS |
| P1.4 test_parity_registry_schemas.py | ✅ | 测试文件存在，22 测试 PASS |
| P1.5 test_parity_routes.py | ✅ | 测试文件存在，2 测试 PASS |
| P1.6 test_parity_stats_mcp.py + get_daily_detail | ✅ | [server.py:167](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/mcp/server.py#L167) 已加 `get_daily_detail` |
| P1.7 test_parity_alembic_metadata.py | ❌ | 文件不存在 |
| P1.8 核心文档/15-扩展性parity例外表.md | ❌ | 文件不存在 |
| Phase 2-4 全部 | ❌ | 未启动 |

## 假设与决策（继承 v2 + 本计划新增）

1. **继承 v2 决策**: sync_entity_type camelCase 兼容 / pull_key 显式 / model_path 动态解析 / 特殊实体豁免 / TDD 纪律
2. **P1.7 tmp_path 适配**: conftest.py 已覆盖 tmp_path 返回 `tests/` 目录。参考已通过的 [test_alembic.py:44](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/tests/test_alembic.py#L44) 模式（`db_path = tmp_path / "alembic_test.db"`），P1.7 用 `tmp_path / "parity_test.db"` 应可工作
3. **P1.7 Base.metadata 时机**: conftest 的 `_isolate_env` autouse 会 reload `app.db.base` 并清空 metadata 再重新填充。P1.7 测试函数体内必须**在 _isolate_env 已运行后** import `Base` 和 `app.models`，确保看到完整 18 表
4. **uv run vs venv 直跑**: 用 `.venv\Scripts\python.exe -m pytest` 直跑，避免 `uv run` 触发 re-sync 卸载手动安装的包（fastmcp-slim）
5. **Phase 2 顺序不可并行**: P2.1→P2.2→P2.3→P2.4 修改同一组文件（entities.py/builtin.py/sync.py），P2.5 依赖 P2.1 的 list_soft_delete，强串行
6. **Phase 3/4 可并行派发**: Phase 3 (scripts/) 与 Phase 4 (stats_spec.py + test_parity_stats_mcp.py 重构) 文件无冲突，可派发两个并行 agent
7. **agent-browser skill 本阶段不用**: 本任务是后端 TDD，agent-browser 适用于 Phase E 部署后的端到端验证。本计划用 pytest 而非浏览器测试
8. **test-driven-development skill 贯穿**: 每个新功能先写一个失败测试，观察失败原因正确，再写最小实现

## 执行路径

### Phase 1 收尾（3 步，顺序）

#### P1.7 Alembic metadata parity test

**RED+GREEN 合并**: 直接照搬 v2 计划代码，应直接 PASS（gate test）。

**新建** [tests/test_parity_alembic_metadata.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/tests/test_parity_alembic_metadata.py)：
- `_alembic_cfg()` — 复用 [test_alembic.py:19-35](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/tests/test_alembic.py#L19-L35) 模式
- `test_alembic_head_tables_match_metadata(tmp_path)` — alembic upgrade head 后表集合 == Base.metadata 表集合

**关键实现细节**:
```python
def test_alembic_head_tables_match_metadata(tmp_path: Path):
    from app.db.base import Base
    from app.models import *  # noqa: F401, F403 - register all models
    from app.db.models import meta  # noqa: F401

    cfg = _alembic_cfg()
    db_path = tmp_path / "parity_test.db"
    engine = create_engine(f"sqlite:///{db_path.as_posix()}")
    with engine.begin() as conn:
        cfg.attributes["connection"] = conn
        command.upgrade(cfg, "head")

    alembic_tables = set(inspect(engine).get_table_names()) - {"alembic_version"}
    metadata_tables = set(Base.metadata.tables.keys())
    engine.dispose()

    assert not (metadata_tables - alembic_tables), f"In metadata but not Alembic: {metadata_tables - alembic_tables}"
    assert not (alembic_tables - metadata_tables), f"In Alembic but not metadata: {alembic_tables - metadata_tables}"
```

**验证命令**:
```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
.venv\Scripts\python.exe -m pytest tests/test_parity_alembic_metadata.py -v --capture=no -p no:cacheprovider
```

**通过标准**: 测试 PASS。如失败说明 Alembic 迁移链有遗漏（需补 migration，但预计直接通过）。

#### P1.8 Parity 例外文档

**新建** [核心文档/15-扩展性parity例外表.md](file:///e:/Development/MyAwesomeApp/PomodoroXII/核心文档/15-扩展性parity例外表.md)，内容照搬 v2 计划 lines 528-565（Schema/Sync/Trash/Stats 四类例外）。

#### Phase 1 验收

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
.venv\Scripts\python.exe -m pytest tests/test_parity_registry_sync.py tests/test_parity_registry_orm.py tests/test_parity_registry_schemas.py tests/test_parity_routes.py tests/test_parity_stats_mcp.py tests/test_parity_alembic_metadata.py tests/test_mcp_server.py tests/test_registry.py -v --capture=no -p no:cacheprovider
```

**通过标准**: 6 个 parity test 文件全 PASS（除 P1.2 第 3 个测试 SKIP）；现有 361 测试无回归。

### Phase 2: EntitySpec 扩展 + registry 派生 sync/trash（6 步，强顺序）

> **风险等级**: 高（修改 SyncService 核心，~30 个 sync 测试为回归基线）
> **不可并行**: P2.1-P2.5 修改同一组文件，P2.6 依赖 P2.2 完成

#### P2.1 EntitySpec 扩展 7 字段 + 2 effective_* 属性

**RED**: 新建 `tests/test_entity_spec_extension.py`，5 测试（照搬 v2 lines 595-669）：
- sync_entity_type / pull_key / delete_strategy 字段
- effective_sync_entity_type fallback / effective_pull_key fallback

**GREEN**: 修改 [entities.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/entities.py) 追加 7 字段（route_prefix/service_path/schema_module/schema_prefix/delete_strategy/route_enabled/mcp_schema_enabled）+ 2 effective_* 属性。

**注意**: EntitySpec 是 frozen dataclass，新增字段必须有默认值，否则已注册的 20 个 spec 实例化会失败。

#### P2.2 为 14 个 sync 实体声明 camelCase + pull_key + delete_strategy

**修改** [builtin.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/builtin.py)，按 v2 lines 717-732 映射表补全 14 实体的 `pull_key` 字段（`sync_entity_type` 7 个 camelCase 已在 P1.2 补齐，剩 7 个 snake_case == name 的也要补 pull_key）。同时为 3 个 soft_delete 实体声明 `delete_strategy`。

**验收**: `tests/test_registry.py` + `tests/test_parity_registry_sync.py` 全绿（第 3 个测试仍 SKIP，P2.6 才启用）。

#### P2.3 实现 build_sync_registry() + 测试

**RED**: 新建 `tests/test_build_sync_registry.py`，5 测试（照搬 v2 lines 779-822）。

**GREEN**: 新建 [app/registry/sync_registry.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/registry/sync_registry.py)：
```python
def build_sync_registry() -> dict[str, dict[str, Any]]:
    result: dict[str, dict[str, Any]] = {}
    for spec in REGISTRY.list_sync_enabled():
        entity_type = spec.effective_sync_entity_type
        model = resolve_model(spec)
        pull_key = spec.effective_pull_key
        result[entity_type] = {"model": model, "pull_key": pull_key, "spec": spec}
    return result
```

#### P2.4 SyncService 切换到派生 registry（关键回归点）

**修改** [app/services/sync.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/sync.py)：
- 删除 lines 19-33 的 14 行硬编码 ORM imports（**保留** `from app.models.note import Note` 和 `from app.models.tombstone import Tombstone`）
- 删除 lines 51-66 的 `ENTITY_REGISTRY` dict
- 改为 `from app.registry.sync_registry import build_sync_registry` + `ENTITY_REGISTRY: dict[str, dict[str, Any]] = build_sync_registry()`

**关键回归命令**（必须全绿）:
```powershell
.venv\Scripts\python.exe -m pytest tests/test_sync_service.py tests/test_sync_integration.py tests/test_sync_routes.py tests/test_sync_safety.py tests/test_sync_indexes.py tests/test_sync_cursor_pagination.py tests/test_sync_note_content.py tests/test_sync_updated_at_indexes.py tests/test_phase_c_completion.py -v --capture=no -p no:cacheprovider
```

#### P2.5 Trash registry 化

**修改** [app/routes/v1/trash.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/routes/v1/trash.py)：
- 删除 lines 23-25 的 3 行 ORM imports（Note/Folder/QuickNote）
- 删除 lines 37-41 的 `_ENTITY_MAP` dict
- 改为 `from app.registry import REGISTRY` + `from app.registry.resolve import resolve_model` + `_ENTITY_MAP = {spec.name: resolve_model(spec) for spec in REGISTRY.list_soft_delete()}`

**注意**: `list_trash` 端点中针对 Note/Folder/QuickNote 的硬编码 SELECT 保留（每种的 title 提取逻辑不同，YAGNI）。但需在文件顶部重新 import Note/Folder/QuickNote（因为 list_trash 用到这些类）。

**回归**: `tests/test_trash_routes.py` 全部 PASS。

#### P2.6 启用 P1.2 第 3 个测试

**修改** `tests/test_parity_registry_sync.py`：删除 `test_sync_registry_pull_keys_match` 中的 `pytest.skip(...)`。

#### Phase 2 验收

```powershell
.venv\Scripts\python.exe -m pytest tests/test_parity_*.py tests/test_build_sync_registry.py tests/test_entity_spec_extension.py tests/test_sync_service.py tests/test_sync_integration.py tests/test_sync_routes.py tests/test_trash_routes.py tests/test_registry.py tests/test_mcp_server.py -v --capture=no -p no:cacheprovider
```

### Phase 3: 脚手架与迁移流程（3 步，可派发 agent）

> **可派发**: 此 Phase 与 Phase 4 文件无冲突，可派发给 Tooling/Test agent 并行执行

#### P3.1 scaffold_entity.py 脚手架

**新建** `backend/scripts/scaffold_entity.py`（需先创建 `scripts/` 目录）。

**实现策略**: 纯字符串拼接（不引入 jinja2 依赖，避免污染 pyproject.toml）。

**MVP 范围**: 只支持 DB-only + hard_tombstone 实体。Note/Folder/Relation 类特殊实体拒绝生成，提示 `--custom-service`。

**CLI 接口**:
```
python scripts/scaffold_entity.py --name goal --class-name Goal --table-name goals \
    --route-prefix goals --fields "title:string,due_date:datetime,completed:boolean" \
    [--dry-run]
```

**输出文件草案**（dry_run=True 时打印到 stdout，否则写入磁盘）:
- `app/models/<name>.py`（含 SyncMixin + FieldSpec 对齐）
- `app/schemas/<name>.py`（Create/Update/Response）
- `app/services/<name>.py`（CRUD Service）
- `app/routes/v1/<plural>.py`（Router）
- `app/registry/builtin.py` 的 EntitySpec 追加片段（打印到 stdout，需手工合并）
- `app/routes/v1/__init__.py` 的 include_router 追加片段（打印到 stdout）
- Alembic revision 草案（打印到 stdout）
- 基础测试草案（打印到 stdout）

**TDD**:
- RED: `tests/test_scaffold_entity.py` — 验证 `dry_run=True` 输出包含 8 个文件块标记（`# --- file: app/models/goal.py ---` 等）
- GREEN: 实现 scaffold_entity.py

#### P3.2 check_entity_consistency.py

**新建** `backend/scripts/check_entity_consistency.py`，调用 pytest 跑所有 parity test：
```python
import subprocess, sys
result = subprocess.run(
    [sys.executable, "-m", "pytest", "tests/test_parity_*.py", "-v",
     "--capture=no", "-p", "no:cacheprovider"],
    cwd=backend_dir,
)
sys.exit(result.returncode)
```

**TDD**: 此脚本本身无测试，作为 CI 入口工具。验证方式 = 直接运行 `python scripts/check_entity_consistency.py` 看退出码。

#### P3.3 Goal dry-run spike

**步骤**:
1. 运行 `python scripts/scaffold_entity.py --name goal --class-name Goal --table-name goals --route-prefix goals --fields "title:string,due_date:datetime,completed:boolean" --dry-run`
2. 检查 stdout 输出 8 个文件块
3. **不合并到主代码库** — 这是 spike，验证完即结束（不写测试，不持久化）

**通过标准**: dry-run 输出结构完整，无明显语法错误。

### Phase 4: Stats/MCP 注册化（2 步，可派发 agent）

> **可派发**: 此 Phase 与 Phase 3 文件无冲突，可派发给 Stats/MCP agent 并行执行

#### P4.1 StatSpec 设计 + 测试

**RED**: 新建 `tests/test_stat_spec.py`，3 测试（照搬 v2 lines 1028-1058）：
- STAT_SPECS 覆盖所有 REST 端点
- STAT_SPECS 覆盖所有 MCP 工具
- route_path 和 mcp_tool 配对一致

**GREEN**: 新建 [app/services/stats_spec.py](file:///e:/Development/MyAwesomeApp/PomodoroXII/backend/app/services/stats_spec.py)，定义 `StatSpec` dataclass + `STAT_SPECS` tuple（7 个维度，照搬 v2 lines 1085-1133）。

#### P4.2 增强 parity test 用 STAT_SPECS 驱动

**修改** `tests/test_parity_stats_mcp.py`：用 `STAT_SPECS` 替代硬编码 `EXPECTED_MAPPING`（照搬 v2 lines 1146-1156）。

**注意**: FastMCP 工具的 introspection 方式需实际验证。`_mcp_tool` 属性可能不准确，可改用 `mcp._tool_manager._tools` 或 `inspect.getmembers` + `callable` 检查。如 introspection 失败，回退到硬编码 EXPECTED_MAPPING（但 STAT_SPECS 仍作为单一来源，EXPECTED_MAPPING 从 STAT_SPECS 派生）。

#### Phase 4 验收

```powershell
.venv\Scripts\python.exe -m pytest tests/test_stat_spec.py tests/test_parity_stats_mcp.py -v --capture=no -p no:cacheprovider
```

### 全局验收 + 收尾

#### 全局测试

```powershell
cd e:\Development\MyAwesomeApp\PomodoroXII\backend
.venv\Scripts\python.exe -m pytest --capture=no -p no:cacheprovider -q
```

**通过标准**:
1. 361 + 新增测试全绿（预计 ~390 测试）
2. `SyncService.ENTITY_REGISTRY` 由 `build_sync_registry()` 派生
3. `trash._ENTITY_MAP` 由 `REGISTRY.list_soft_delete()` 派生
4. `EntitySpec` 含 7 新字段 + 2 effective_* 属性
5. 6 个 parity test 文件 + 3 个新测试文件（entity_spec_extension/build_sync_registry/stat_spec/scaffold_entity）
6. MCP `get_daily_detail` 工具存在且可执行
7. `scaffold_entity.py` + `check_entity_consistency.py` 可运行
8. `核心文档/15-扩展性parity例外表.md` 存在

#### 更新 project_memory.md

更新 `c:\Users\20564\.trae-cn\memory\projects\-e-Development-MyAwesomeApp-PomodoroXII\project_memory.md`：
- 新增 Phase F-1（扩展性 4.5 星）= 100%
- 测试总数 361 → ~390
- 关键文件位置追加：`app/registry/resolve.py`、`app/registry/sync_registry.py`、`app/services/stats_spec.py`、`scripts/scaffold_entity.py`、`scripts/check_entity_consistency.py`、`核心文档/15-扩展性parity例外表.md`

#### Git commit

```bash
git add -A
git commit -m "feat: extensibility 4.5-star improvement (parity gates + registry-derived sync/trash + StatSpec + scaffold)"
```

**不 push**（除非用户要求）。

## 三路并行 agent 派发建议

> 仅在 Phase 2 完成后（Phase 1 + Phase 2 必须单线顺序执行）考虑派发

### Agent A: Tooling/Test（Phase 3）
- 范围: P3.1 + P3.2 + P3.3
- 文件: `backend/scripts/scaffold_entity.py`、`backend/scripts/check_entity_consistency.py`、`backend/tests/test_scaffold_entity.py`
- 不冲突: 不动 app/ 下任何文件
- 验收: `python scripts/check_entity_consistency.py` 退出码 0

### Agent B: Stats/MCP（Phase 4）
- 范围: P4.1 + P4.2
- 文件: `backend/app/services/stats_spec.py`、`backend/tests/test_stat_spec.py`、`backend/tests/test_parity_stats_mcp.py`
- 不冲突: 不动 scripts/ 下任何文件
- 验收: `tests/test_stat_spec.py` + `tests/test_parity_stats_mcp.py` 全绿

### 主线（不派发）: Phase 1 + Phase 2
- 原因: P2.1-P2.5 强耦合同组文件，并行会冲突；Phase 1 是 Phase 2 的前置

## 风险与缓解

| 风险 | 缓解 |
|---|---|
| P1.7 tmp_path 沙箱限制 | 参考 test_alembic.py 已能跑（同模式） |
| P2.4 SyncService 切换破坏 30 个 sync 测试 | 切换前先跑一遍 sync 测试基线，切换后立即全量回归 |
| P2.5 Trash 切换后 list_trash 仍需 Note/Folder/QuickNote 类 | 保留文件顶部 3 行 import，只删 _ENTITY_MAP dict |
| P4.2 FastMCP introspection 失败 | 回退方案：EXPECTED_MAPPING 从 STAT_SPECS 派生 |
| Phase 3 jinja2 依赖 | 不引入 jinja2，纯字符串拼接 |
| uv run 卸载 fastmcp-slim | 全程用 `.venv\Scripts\python.exe -m pytest` |

## 文件清单

### 新建文件（11 个，扣除已完成的 P1.3-P1.6）

| 路径 | 用途 | 所属任务 |
|---|---|---|
| `backend/tests/test_parity_alembic_metadata.py` | Alembic vs metadata parity | P1.7 |
| `核心文档/15-扩展性parity例外表.md` | 例外表文档 | P1.8 |
| `backend/tests/test_entity_spec_extension.py` | EntitySpec 扩展字段测试 | P2.1 |
| `backend/app/registry/sync_registry.py` | build_sync_registry() | P2.3 |
| `backend/tests/test_build_sync_registry.py` | build_sync_registry 测试 | P2.3 |
| `backend/scripts/scaffold_entity.py` | 实体脚手架 CLI | P3.1 |
| `backend/scripts/check_entity_consistency.py` | 一致性检查脚本 | P3.2 |
| `backend/tests/test_scaffold_entity.py` | 脚手架测试 | P3.1 |
| `backend/app/services/stats_spec.py` | StatSpec + STAT_SPECS | P4.1 |
| `backend/tests/test_stat_spec.py` | StatSpec 测试 | P4.1 |

### 修改文件（5 个）

| 路径 | 改动 | 所属任务 |
|---|---|---|
| `backend/app/registry/entities.py` | 加 7 字段 + 2 effective_* 属性 | P2.1 |
| `backend/app/registry/builtin.py` | 14 实体补 pull_key + 3 实体补 delete_strategy | P2.2 |
| `backend/app/services/sync.py` | ENTITY_REGISTRY 改派生 | P2.4 |
| `backend/app/routes/v1/trash.py` | _ENTITY_MAP 改派生 | P2.5 |
| `backend/tests/test_parity_registry_sync.py` | 启用第 3 测试 | P2.6 |
| `backend/tests/test_parity_stats_mcp.py` | 用 STAT_SPECS 驱动 | P4.2 |

## 执行纪律

1. **每个任务严格 TDD**: RED → 验证失败原因正确 → GREEN → 验证通过 → 必要时 REFACTOR
2. **不批量写测试**: 每次只写一个失败测试
3. **回归优先**: 每完成一个 GREEN 立即跑相关回归测试
4. **顺序推进 Phase 1 + Phase 2**: 不可并行（共享文件冲突）
5. **Phase 3/4 可并行派发**: 文件无冲突
6. **沙箱适配**: 所有 pytest 命令加 `--capture=no -p no:cacheprovider`
7. **venv 直跑**: 用 `.venv\Scripts\python.exe -m pytest`，不用 `uv run pytest`
8. **不动 FS 层**: `file_system/engine/trash_ops.py` 独立于 DB trash
9. **特殊实体豁免**: Note/Folder/Relation 在 parity test 中显式声明例外
10. **不 push**: 除非用户明确要求
