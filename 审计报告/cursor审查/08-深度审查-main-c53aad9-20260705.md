# PomodoroXII 深度审查 — main @ c53aad9（Phase C 收官）

> **审查时间：** 2026-07-05 晚间（PR #13 合并后）  
> **审查工具：** Cursor Agent + CBM 知识图谱（full re-index）  
> **有效基线：** `main` @ `c53aad9`  
> **CBM 项目名：** `E-Development-MyAwesomeApp-PomodoroXII`  
> **全量测试：** **560 collected / 560 passed**（本机复跑）

---

## 一、执行摘要

**Phase C Sync 引擎正式 100% 收官。** 当日累计合并 **13 个 PR**（#2–#13），完成 MCP 收口、部署基线、实体/墓碑双游标分页、`conflict_remote` 契约修正、仓库卫生与 deps 对齐。后端从「同步有已知分页 bug」进入 **push/pull 契约闭环** 状态。

最大缺口仍是：**前端零进度**、**生产安全头未做**、**根 README 缺失**。对个人公网单用户场景，限流可继续延后。

| 维度 | 评分 | 较午后变化 |
|------|------|------------|
| 架构合规（三条铁律） | ★★★★★ | — |
| Sync 引擎 | ★★★★★ | ↑ 墓碑 since_id 闭环 |
| MCP / Agent | ★★★★☆ | fastmcp lock 对齐 3.x |
| 部署 / 运维 | ★★★★☆ | — |
| 安全防护 | ★★★☆☆ | 仍无限流/安全头 |
| 测试 | ★★★★☆ | 560 passed |
| 可交付产品 | ★★☆☆☆ | 无 UI |
| **后端综合** | **★★★★☆+** | Phase C 收官 |

---

## 二、main 合并轨迹（截至 c53aad9）

```
c53aad9  Merge PR #13  feat: tombstone_since_id cursor
014ec05  Merge PR #12  chore: fastmcp lock >=3.0
ed8a54e  Merge PR #11  chore: gitignore tool artifacts
6b3baca  Merge PR #10  fix: conflict_remote → applied
77e890d  Merge PR #9   docs: Cursor audit reports
4ac3bdb  Merge PR #8   fix: since_id multipage hotfix
6ea4021  Merge PR #7   feat: since_id composite cursor
e6f532e  Merge PR #6   ci: GHCR + docker-compose + DEPLOY
…        PR #2–#5      MCP lifespan / lint / spec / WIP
```

---

## 三、8 阶段进度

```
Phase A  基础设施                    ████████████████████ 100%
Phase B  业务层                      ████████████████████ 100%
Phase C  Sync 引擎                    ████████████████████ 100%  ← 收官
Phase D  笔记/搜索/回收站              ███████████████░░░░░  75%
Phase E  MCP/备份/导出                 ███████████████░░░░░  75%
Phase F  React 19 前端                  ░░░░░░░░░░░░░░░░░░░░   0%
Phase G  数据迁移 / E2E                ███░░░░░░░░░░░░░░░░░  15%
Phase H  部署 / CI/CD                  ██████████████░░░░░░  70%
────────────────────────────────────────────────────────────
仅后端 (A–E,H)                       ≈ 88%
含前端完整产品 (A–H)                 ≈ 50%
```

### Phase C 明细（全部 ✅）

| 项 | PR |
|----|-----|
| 5 道 sync 安全检查 | 早期 |
| 毫秒时间戳 + alembic 006 | 早期 |
| `(since, since_id)` 实体复合游标 | #7 |
| 同 ts 多页 hotfix（`latest_entity_ts/id`） | #8 |
| `conflict_remote` → `applied[].resolution="remote"` | #10 |
| `(tombstone_since_id, next_tombstone_since_id)` 墓碑游标 | #13 |
| 全局 per-entity 独立 cursor | 已知限制，低优先 |

### Sync pull 游标契约（客户端必读）

每轮 pull/full 直到 `has_more == false`，持久化并回传：

```
since, since_id, tombstone_since_id
```

响应：`next_since`, `next_since_id`, `next_tombstone_since_id`

Push：`conflicts` 仅含被拒绝事件；LWW 远端胜出查 `applied[].resolution == "remote"`。

---

## 四、CBM 知识图谱（本次 re-index）

| 指标 | 数值 |
|------|------|
| 模式 | `full` + `persistence=true` |
| 节点 | **2,408** |
| 边 | **10,167** |
| Python 文件 | 196 |
| Route 节点 | 115 |
| 测试→app 调用边 | 765 |
| 热点 | `BaseService.get` (fan-in 112)、`SyncService.push/pull`、Note FS |

**架构簇（CBM）：** SyncService 簇、NoteService+FS 簇、Stats 簇、Tombstone+Relation 簇耦合清晰，与三条铁律（routes 薄 / services 厚 / registry 单源）一致。

**产物：** `.codebase-memory/graph.db.zst` 已写入（`artifact.json` 在 `.gitignore` 中忽略）。

---

## 五、代码库实况

| 指标 | 数值 |
|------|------|
| `backend/app/` 文件 | 110 |
| Alembic 版本 | 7（001–006 + phase_b） |
| 同步实体（registry） | 14 类 |
| REST v1 路由装饰器 | ~71（18 模块） |
| MCP tools | 12（+ resources/prompts） |
| pytest 收集 | **560** |
| 根 `README.md` | ❌ 不存在 |
| `DEPLOY.md` | ✅ `backend/DEPLOY.md` |
| Docker / GHCR CI | ✅ |

### 中间件现状

仅 `RequestIdMiddleware` + `CORSMiddleware`。**无** `SecurityHeadersMiddleware`、**无** rate limit。

### BackupService

`app/file_system/backup.py` 已实现 `sqlite3.backup()` Online Backup，**未接入** `main.py` lifespan。

### 笔记搜索

- FS 层：`search_ops.py` 有 FTS5 实现 + 测试
- REST：**无** `GET /notes/search` 端点（Phase D 缺口）

### PUT 覆盖缺口（REST）

有 PUT：`tasks`, `folders`, `notes`, `quick_notes`, `settings`  
无 PUT：`sessions`, `habits`, `schedules`, `time_blocks`, `reflections`（仅 POST/GET/DELETE）

---

## 六、问题分级

### P0 — 公网部署前

| # | 项 | 状态 |
|---|-----|------|
| 1 | 安全响应头中间件（HSTS/X-Content-Type-Options 等） | ❌ |
| 2 | 根 `README.md`（clone/run/test/deploy 入口） | ❌ |
| 3 | HTTPS + 强 `POMODOROXII_SECRET_KEY`（≥32B） | 文档已有，运维待做 |

### P1 — 功能完整性

| # | 项 | 状态 |
|---|-----|------|
| 4 | `GET /notes/search`（FTS5 REST 暴露） | ❌ |
| 5 | `BackupService` 接入 lifespan 定时备份 | ❌ |
| 6 | GHCR 镜像 `docker pull` + smoke 验证 | 未实测 |
| 7 | Note 路径 `conflict_remote` 单测 | 可选 |

### P2 — 产品化

| # | 项 | 状态 |
|---|-----|------|
| 8 | Phase F 前端脚手架（Next.js 15 + auth + space） | 0% |
| 9 | sessions/habits 等 PUT 路由补齐 | 部分 |
| 10 | 认证限流 | 个人场景可延后 |

### 已知限制（接受）

- 全局 `since_id` / `tombstone_since_id` 跨 14 实体类型共用（非 per-entity-type cursor）
- `pull()` result dict 未预置 `next_tombstone_since_id: ""`（Pydantic 层有默认）

---

## 七、推荐 Sprint（Post–Phase C）

### Sprint 1 — 可部署加固（~1–2 天）

1. `codex/security-headers` — SecurityHeadersMiddleware + 测试
2. 根 `README.md` — 指向 `backend/DEPLOY.md`、env、pytest
3. 可选：`docker pull ghcr.io/...` smoke

### Sprint 2 — Phase D 收尾（~2–3 天）

1. `GET /api/v1/notes/search?q=`
2. `BackupService` lifespan 挂钩
3. PUT 路由补齐（按前端需求优先级）

### Sprint 3 — Phase F 起步（~1 周）

1. Next.js 15 脚手架 + JWT auth flow
2. Space switcher + tasks 列表只读页
3. Sync client SDK 原型（三游标循环）

---

## 八、裁定

| 维度 | 判定 |
|------|------|
| Phase C Sync | ✅ **正式完成** |
| 后端可继续前端 | ✅ |
| 公网部署就绪 | ⚠️ 需安全头 + README |
| CBM 索引 | ✅ 2,408 nodes 已更新 |

**Ground truth：** `main` @ `c53aad9` + 本目录报告 + CBM `E-Development-MyAwesomeApp-PomodoroXII`。

---

*Cursor Agent · 2026-07-05 · post PR #13 merge*
