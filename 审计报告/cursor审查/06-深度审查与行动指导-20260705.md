# PomodoroXII 深度审查与后续行动指导

> **审查时间：** 2026-07-05 晚间  
> **审查工具：** Cursor Agent + CBM 代码知识图谱  
> **有效基线：** `main` @ `77e890d` + 待合并 `5e4dfb4`（conflict_remote）  
> **全量测试：** 558 passed（含 conflict_remote 分支）

---

## 一、执行摘要

PomodoroXII 后端在 **单日完成 9 个 PR 量级交付**（MCP 收口、部署基线、since_id 分页、hotfix、docs、conflict_remote）。同步引擎从「有已知 bug」进入 **push/pull 契约基本闭环** 状态。最大缺口仍是 **前端零进度** 与 **生产安全加固未做**。

| 维度 | 评分 | 变化（较上午） |
|------|------|----------------|
| 架构合规 | ★★★★★ | — |
| Sync 引擎 | ★★★★★ | ↑（cursor + conflict_remote） |
| MCP/Agent | ★★★★☆ | — |
| 部署/运维 | ★★★★☆ | — |
| 安全防护 | ★★★☆☆ | 仍无限流/安全头 |
| 测试 | ★★★★☆ | 558 函数 |
| 可交付产品 | ★★☆☆☆ | 无 UI |
| **后端综合** | **★★★★☆+** | Sync 收口显著提升 |

---

## 二、当日 main 合并轨迹

```
77e890d  Merge PR #9   docs: Cursor audit reports
4ac3bdb  Merge PR #8   fix: since_id multipage hotfix
6ea4021  Merge PR #7   feat: since_id composite cursor
e6f532e  Merge PR #6   ci: GHCR + docker-compose + DEPLOY
...      PR #2–#5      MCP lifespan / lint / spec / WIP
```

**待合并：**

```
5e4dfb4  fix(sync): conflict_remote reclassification  ← codex/sync-conflict-remote-reclassify
```

---

## 三、8 阶段进度（更新）

```
Phase A  基础设施                    ████████████████████ 100%
Phase B  业务层                      ████████████████████ 100%
Phase C  Sync 引擎                    ███████████████████░  99.5%
Phase D  笔记/搜索/回收站              ███████████████░░░░░  75%
Phase E  MCP/备份/导出                 ███████████████░░░░░  75%
Phase F  React 19 前端                  ░░░░░░░░░░░░░░░░░░░░   0%
Phase G  数据迁移 / E2E                ███░░░░░░░░░░░░░░░░░  15%
Phase H  部署 / CI/CD                  ██████████████░░░░░░  70%
────────────────────────────────────────────────────────────
仅后端 (A–E,H)                       ≈ 85%  ↑
含前端完整产品 (A–H)                 ≈ 48%  ↑
```

### Phase C 明细

| 项 | 状态 |
|----|------|
| 5 道 sync 安全检查 | ✅ |
| 毫秒时间戳 + alembic 006 | ✅ |
| `(since, since_id)` 复合游标 | ✅ PR #7 |
| 同 ts 超过 2 页 hotfix | ✅ PR #8 |
| `conflict_remote` → applied | ✅ 待合并 PR |
| Tombstone since_id 分页 | ⏳ |
| 全局 per-entity cursor | ⏳ 低优先 |

---

## 四、代码库实况

| 指标 | 数值 |
|------|------|
| `app/` Python 文件 | 116 |
| 测试函数 | **495**（静态）/ **558**（运行时含参数化） |
| REST 路由模块 | 15 |
| MCP 工具 | 13（`EXPECTED_MCP_TOOLS` 门禁） |
| 注册实体 | 20 |
| Alembic 迁移 | 7 |
| README | ❌ 无（有 `backend/DEPLOY.md`） |
| frontend/ | ❌ 无 |

### REST 缺口（未变）

| 类别 | 状态 |
|------|------|
| 完整 CRUD | tasks, folders, notes, quick-notes, trash, settings |
| 无 PUT | sessions, reflections, schedules, time-blocks, habits |
| 未暴露 HTTP | notes search (FTS5), export, consistency, relations |
| sync-only | memo_comment |

### 运维/安全缺口

| 项 | 状态 |
|----|------|
| GHCR + docker-compose + DEPLOY | ✅ |
| 认证限流 | ❌ |
| 安全响应头 | ❌ |
| BackupService 调度 | ❌ 代码有，lifespan 未接 |
| pytest-cov | ❌ |

---

## 五、工作区卫生

```text
M backend/uv.lock          ← fastmcp 2→3，单独 PR 或还原
?? .codebase-memory/       ← 一般不提交
?? backend/.pytest-tmp/    ← 加入 .gitignore
?? .trae/documents/...     ← 计划文档，可选归档
```

**conflict_remote PR 合并前：** 确保 `uv.lock` 不在该 PR 中。

---

## 六、后续行动指导

### 立即（合并 conflict_remote 后）

| # | 动作 | 分支 | 估时 |
|---|------|------|------|
| 1 | 合并 PR `codex/sync-conflict-remote-reclassify` | — | — |
| 2 | `git checkout main && git pull` | — | 5 min |
| 3 | CBM 重索引（已执行，见 §七） | — | — |

### Sprint 1（本周）— 阶段 6：生产安全 【P0】

| # | 任务 | 分支 | 验收 |
|---|------|------|------|
| 6a | slowapi 限流 `/auth/login` 5/min/IP | `codex/security-rate-limit` | `test_rate_limit.py` |
| 6b | `SecurityHeadersMiddleware` | `codex/security-headers` 或同上 | 响应头断言 |
| 6c | 根目录 `README.md` | `docs/readme` | 指向 DEPLOY.md |
| 6d | `docker pull` + compose smoke | 无代码 | health 200 |

**铁律：** 一主题一分支一 PR。

### Sprint 2（下周）— 阶段 7：前端 MVP 【P1】

| # | 任务 |
|---|------|
| 7a | `frontend/` Next.js 15 + React 19 脚手架 |
| 7b | Master login + space 切换 |
| 7c | Tasks 列表（只读即可） |
| 7d | Dexie Proxy spike（可后置） |

### Sprint 3（穿插）— Phase D/C 收口 【P2】

| # | 任务 |
|---|------|
| D-1 | `GET /notes/search` 暴露 FTS5 |
| D-2 | 5 类资源补 PUT |
| C-2 | Tombstone `(deleted_at, entity_id)` since_id |
| E-1 | BackupService 接入 lifespan |
| C-3 | Note 路径 `conflict_remote` 测试 |

### 不建议现在做

- MCP mount FastAPI（独立进程够用）
- 全局 per-entity cursor 重构（无真实 bug 驱动）
- `uv.lock` fastmcp 与功能 PR 混提

---

## 七、CBM 索引状态

| 项 | 值 |
|----|------|
| 项目 | `E-Development-MyAwesomeApp-PomodoroXII`（全仓库） |
| 最近索引 | 2026-07-05 晚间 |
| 含 conflict_remote | ✅（当前工作区） |
| 含审计报告 | ✅ |

查询时使用项目名 `E-Development-MyAwesomeApp-PomodoroXII`，勿用旧的 `PomodoroXII-backend`。

---

## 八、给 Agent 的下一条 Prompt（阶段 6a）

```markdown
## 任务：阶段 6a — 认证限流

基线：main（含 conflict_remote PR 合并后）
分支：codex/security-rate-limit

范围：
- slowapi POST /api/v1/auth/login 5/min/IP
- main.py limiter 注册
- tests/test_rate_limit.py
- 禁止改动 sync/MCP/前端

验证：专项 + auth 回归 + 全量 pytest + ruff
```

---

## 九、里程碑预测

| 里程碑 | 条件 | 预计进度 |
|--------|------|----------|
| Sync 引擎闭环 | conflict_remote 合并 | **本周末** |
| 可公网部署后端 | + 阶段 6 安全 | Sprint 1 后 ≈ 88% |
| 用户可用 MVP | + Phase F 前端骨架 | Sprint 2 后 ≈ 55% 产品 |

---

## 十、审查结论

PomodoroXII 后端工程质量处于高位，**Sync 是今日最大收获**。conflict_remote PR 合并后，Phase C 可宣告实体侧收口。下一战场明确：**安全加固 → 前端 MVP**，其余为增强项。

---

*Cursor Agent · 2026-07-05 晚间 · CBM 索引后审查*
