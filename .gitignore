# ===================================================================
# PomodoroXII .gitignore
# ===================================================================

# -------------------------------------------------------------------
# Python
# -------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# -------------------------------------------------------------------
# Virtual environments
# -------------------------------------------------------------------
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# -------------------------------------------------------------------
# Testing / coverage
# -------------------------------------------------------------------
.pytest_cache/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.ruff_cache/
.mypy_cache/
.dmypy.json
dmypy.json
pyright/
pytest_*.log
pytest_run.log
# Run-scoped backend test artifacts; CI/workspace lifecycle owns cleanup.
backend/.test-artifacts/
# Legacy temp root from the previous tmp_path override.
backend/tests/.tmp/
# pytest tmp dirs (uv lock files, ad-hoc test workspace)
.pytest-tmp/

# -------------------------------------------------------------------
# Project runtime data (must never be committed)
# -------------------------------------------------------------------
backend/data/
backend/data/meta.db
backend/data/meta.db-journal
backend/data/spaces/
backend/data/spaces/**/*.db
backend/data/spaces/**/*.db-journal
backend/data/spaces/**/*.db-wal
backend/data/spaces/**/*.db-shm
backend/data/notes/
*.db
*.db-journal
*.db-wal
*.db-shm

# -------------------------------------------------------------------
# Secrets / environment
# -------------------------------------------------------------------
.env
.env.local
.env.*.local
backend/.env
*.pem
*.key
secrets.json
credentials.json

# -------------------------------------------------------------------
# IDE / editor
# -------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# -------------------------------------------------------------------
# Trae / MCP local state (do not commit local agent state)
# -------------------------------------------------------------------
.trae/mcp.json.bak
.trae/mcp.json.before-fix-*
.trae/mcp.json.fixed
.trae/cognee-install.log

.trae/bin/
.trae/mcp.json
.trae/cognee-venv/

# Private project documents and review material. Keep these on local storage and
# private cloud backups; they must not be added to the public repository.
.trae/
docs/
docs/superpowers/
docs/task-space-design/
docs/research/
documents/
核心文档/
审计报告/
output/
*.html
*审查*.md
*审计*.md
*报告*.md
*实施*.md
*行动指导*.md
*深度*.md
*Prompt*.md
*prompt*.md
*plan*.md
*review*.md
*report*.md
*body*.md
overview*.md
/*.md
!README.md
HANDOFF.md
PhaseC-sync-repair-dispatch.md

# NOTE: backend/.codebase-memory/graph.db.zst IS committed — shared index artifact.
# codebase-memory artifact.json contains volatile indexed_at/commit fields — do not commit.
.codebase-memory/artifact.json

# -------------------------------------------------------------------
# WorkBuddy local state (agent memory, do not commit)
# -------------------------------------------------------------------
.workbuddy/
.worktrees/

# -------------------------------------------------------------------
# Local uploads (runtime data, do not commit)
# -------------------------------------------------------------------
.uploads/

# -------------------------------------------------------------------
# Archived / legacy projects (kept locally, excluded from VCS)
# -------------------------------------------------------------------
PomodoroXII-rebuild/
pomodoroxii-deep-review/

# -------------------------------------------------------------------
# Temp / scratch
# -------------------------------------------------------------------
temp/
*.tmp
*.bak
*.log

# -------------------------------------------------------------------
# OS generated
# -------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# --- T6b (2026-08-30): track upstream task-space contracts -------------------
# These sources are read-only upstream spec exports (MANIFEST SHA-256 baselined)
# with no privacy risk.  Tracking them makes external contract-citation review
# reproducible from a clean checkout (see 任务空间核查裁决-2026-08-26 §四).
# Everything else under docs/ and docs/task-space-design/ stays ignored.
!docs/
docs/*
!docs/task-space-design/
docs/task-space-design/*
!docs/task-space-design/sources/
docs/task-space-design/sources/*
!docs/task-space-design/sources/upstream-tip-tip/
!docs/task-space-design/sources/upstream-tip-tip/**

!docs/task-space-design/MANIFEST.md

# --- 2026-09-10: 探针/临时产物（落袋治理，A 阶段）---------------------------
# 这些是排查过程中留下的临时文件，不属于产品代码；留在工作树但不入库。
/tmp/
/tmp-*.txt
/snap6.yaml
# 调试用 Playwright 规格：只允许 focus-offline / task-space 两个正式用例
frontend/e2e/tmp-*.spec.ts
frontend/e2e/debug-*.spec.ts

# --- 2026-09-10: 运行时数据根与依赖目录（落袋治理，A 阶段）-------------------
# ★★★ 最高优先：/data 是真实运行数据根（meta.db + 各 Space 的 space.db），
#   里面有真实工作项与笔记；下方只忽略了 backend/data，根级 /data 此前是**裸露**的，
#   一次 `git add -A` 就会把个人数据推上公开仓库。
/data/
/node_modules/

# 本机 AI/编辑器工具的工作目录（非产品代码）
/.archify/
/.codebase-memory/
/.codex/
/.data.runtime/
/.kc-stage/
/.playwright-cli/

# 探针残留
/nul
/--title
/book.yaml
/canvas-*.yaml
/snap*.yaml

# --- 2026-09-10: 环境变量与密钥备份（落袋治理）-------------------------------
# ★★★ backend/.env.bak-20260831 内含真实密钥且此前**未忽略** —— 一次
#   `git add -A` 就会把它推上公开仓库。下面把整族备份名一并封死。
.env
.env.*
!.env.example
!*.env.example
!.env.local.example
*.env.bak-*
backend/.data.runtime/
frontend/.codebase-memory/
frontend/.playwright-cli/

# --- 2026-09-10: 非产品产物（落袋治理）---------------------------------------
# 方案 A 架构图的视觉核对快照（一次性验收产物，可重新生成）
/方案A-*.visual-check.*
# 个人笔记（不进公开仓库）
/灵感培育日志/
